import { ProfileListingsSkeleton } from "@/components/ProfileListingsSkeleton";

/** Public profile loading skeleton: avatar header + listings grid. */
export default function Loading() {
  return <ProfileListingsSkeleton />;
}
