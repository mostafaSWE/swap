/**
 * Mobile theme bridge — re-exports the shared design tokens from @swap/config
 * so the native app uses the exact same palette/spacing as the web app.
 */
export { colors, radii, spacing } from "@swap/config";
