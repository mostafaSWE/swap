# Swap — Build Plan & Session Tracker
> **Agent: Read this file before touching any code. Update it before ending any session.**
> The full product spec lives in `AGENT_MASTER_PROMPT.md`. This file tracks only progress and continuity.

---

## ▶ RESUME HERE

**Current phase:** **Phase 6 — Polish, Performance & Web Launch (IN PROGRESS).** Phases 1–5 are code-complete & live. **Session 13 (latest)** landed the Phase-6 **production-hardening bundle** — fully additive, **no DB changes**: **error boundaries** (`global-error.tsx` + a catch-all `[locale]/error.tsx` + 5 per-route boundaries, all over a shared `RouteError` reusing the `ErrorState` primitive + `states` i18n), **loading skeletons** (10 new `loading.tsx` + a neutral catch-all, reusing `.skeleton`/`ListingGridSkeleton`, marked `aria-hidden`), **SEO** (`robots.ts`, build-safe `sitemap.ts`, `metadataBase` + locale-aware `generateMetadata` w/ OG/Twitter/`alternates` on the root layout + profile/categories/listings pages), **NestJS rate limiting** (`@nestjs/throttler` global 120/min + 30/min `@Throttle` on the two upload-signing endpoints + `@SkipThrottle` on `/health` + env-gated `trust proxy`), **security headers** (`helmet` + a CORS allowlist replacing `cors:true` on the API; `next.config.mjs` prod-only **CSP** + HSTS/X-Frame-Options/nosniff/Referrer-Policy/Permissions-Policy on the web), and **CI** (`.github/workflows/ci.yml` → `turbo typecheck/lint/build`). Ran a **5-dimension adversarial-review workflow (11 agents) → 6 confirmed (0 refuted) → all 6 fixed**. Typecheck 8/8, web lint clean, web build green. **Remaining Phase-6 code work: Playwright E2E + a full RTL/a11y/perf audit + the 2 deferred Phase-5 lows** (web read-level ban lockout via RLS/middleware; 2h admin session timeout). Outstanding human-only items still open: the one-time **Resend setup** (Session 9) + a two-browser **manual QA pass** (now also covering the admin panel). Remaining low-value divergences (design tokens, mobile nav, category subcats, tsvector search, "open to swap for" picker) stay deferred.
**Last worked on:** **Phase 6 — production-hardening bundle (Session 13, 2026-06-20).** **Web:** new `RouteError` + `global-error.tsx` + a catch-all `[locale]/error.tsx` + per-route `error.tsx` (listings, listings/[id], messages/[conversationId], users/[username], admin/(panel)); 10 new `loading.tsx` (catch-all `[locale]` neutral skeleton + listing-detail/messages/chat/categories/saved/my-listings/admin + shared `ProfileListingsSkeleton` for profile & user); `robots.ts` + `sitemap.ts` (`force-dynamic`, guarded Supabase fetch); `generateMetadata` on `[locale]/layout.tsx` (metadataBase + title template + OG/Twitter/alternates) + profile/categories/listings; `next.config.mjs` `headers()` (prod-only CSP + always-on HSTS/X-Frame-Options/nosniff/Referrer-Policy/Permissions-Policy). **API:** `helmet` + CORS allowlist + env-gated `trust proxy` (`main.ts`); `ThrottlerModule.forRoot`+`APP_GUARD ThrottlerGuard` (`app.module.ts`); `@SkipThrottle` (health) + `@Throttle` (listings `signImage`, proposals `signConfirmation`); deps `+@nestjs/throttler +helmet`. **Root:** CI workflow + `.env.example` (`CORS_ORIGINS`, `TRUST_PROXY`). **6 review findings fixed:** global-error now logs `error` + tags its English line `lang=en/dir=ltr` (Arabic-first kept); CORS uses `||` so an empty `CORS_ORIGINS` no longer blocks every origin; env-gated `trust proxy` so the throttler keys on the real client IP behind a proxy (off by default to avoid XFF spoofing); `aria-hidden` on skeleton roots; shared `ProfileListingsSkeleton` dedup. **Typecheck 8/8, web lint clean, web build green.**

— Prior: **Phase 5 — Admin Panel (Session 12, 2026-06-19).** Migration `0010` (additive): `profiles.is_banned` + `suspended_until` + `suspension_reason`, `admin_actions.ip`; AuthGuard hardened (ban + expiring-suspension); mirrored into `full_setup.sql` + `0001`. **Backend:** `AdminService` self-lockout guard, audit-`logAction` now error-checked + IP-aware, private **notes** (stored as `admin_actions` rows → admin-only readable), **system messages** + **request-edits** (service-role get-or-create conversation + message), `ParseUUIDPipe` on all admin routes, `@Ip()` capture. **Shared:** `Profile`/`AdminAction` types, widened `adminUpdateUserSchema` + `adminUserNoteSchema`/`adminMessageSchema`, REST `addUserNote/messageUser/requestListingEdits`. **Web:** `lib/admin-analytics.ts` (5 cards + sparklines + 5 charts + funnel, date-range-aware), rewritten `lib/admin.ts` (Paged users/listings/reports/audit + `fetchAdminUserDetail` + `effectiveSuspended`), Recharts wrappers (`charts.tsx`), `DateRangePicker`, `url-controls` (search/tabs/pagination), `action-kit` (busy + confirm/text/suspend dialogs with **error surfacing**), `UserActions`/`ListingActions`/`ReportActions`, `(panel)/{page,users,users/[id],listings,reports,audit}` + `(panel)/layout` + `/admin/login`, sidebar Audit Log item, Sheet a11y (role/aria-modal/Escape/focus), full bilingual `admin` namespace (parity 513=513). Added **recharts** dep. Ran a **6-dimension adversarial-review workflow (26 agents) → 19 confirmed (1 refuted; the migration agent died → self-reviewed) → 16 fixed, 2 lows deferred** (dup-conversation race; 2h session timeout). Key fixes: audit-log error swallowing, unchecked participant insert, self-lockout guard, sparkline upper-bound (was bounded by the picker), reports 500-cap blank-pages, dialog/escalation error+refresh handling, web ban-gate in `requireUser`, effective-suspension badges, localized proposal-status + reasons + pagination landmark, actor-IP audit, Warn-user action. **Typecheck 8/8, web lint clean, web build green (all admin routes). `0010` applied to the live DB (session pooler, transaction-wrapped, additive); 4/4 columns verified + `authenticated`-role read of the new columns PASS.**
— Prior: **Email auth (Resend) + profile onboarding (Session 9, 2026-06-19).** Email verification + forgot/reset password now run through **Resend** via a Supabase "Send Email" Auth Hook (`supabase/functions/send-email` Deno edge function → bilingual templates), with `forgot-password`/`reset-password` pages + an `/auth/confirm` `verifyOtp` callback. Plus a `/onboarding` route (name/city/avatar) and `AvatarUpload` (public `avatars` bucket, self-persists `avatar_url`; client validation + live server-side bucket caps). **⚠ Emails will NOT send until you complete the one-time setup below — the code is ready, but the Resend key + hook are configured in Supabase, not in this repo.**

**⚠ REQUIRED manual setup for email (do once):**
1. **Create a Resend API key** at resend.com → API Keys (`re_…`); verify a sending domain (or use `onboarding@resend.dev` for testing).
2. **Deploy the hook function:** `supabase functions deploy send-email --no-verify-jwt` (needs the Supabase CLI linked to the project; or paste the function in Dashboard → Edge Functions).
3. **Set the function secrets** — *this is where the Resend key goes:* `supabase secrets set RESEND_API_KEY=re_… RESEND_FROM="Swap <noreply@yourdomain>" PUBLIC_APP_URL=https://yourapp` (+ `SEND_EMAIL_HOOK_SECRET` from step 4).
4. **Enable the hook:** Dashboard → Authentication → Hooks → **Send Email Hook** → enable → point at the `send-email` function → copy its generated secret into `SEND_EMAIL_HOOK_SECRET` (step 3).
5. **Allowlist redirects:** Dashboard → Authentication → URL Configuration → add `NEXT_PUBLIC_APP_URL` + `<app>/**` to **Redirect URLs**; turn **Confirm email** ON for signup verification. (Local dev: the cloud hook can't reach localhost — test email delivery against a deployed app or a tunnel; the auth *flows* work locally regardless.)

— Prior: **Notifications (Session 8, 2026-06-19)** — the final Phase-3 item; the in-app notification center. New migration **`0008_notifications.sql`**: a `notifications` table fed by **5 SECURITY DEFINER triggers** on the source tables (so a notice fires regardless of write path — backend service, direct RLS write, or the completion RPC): swap_proposals INSERT → recipient `proposal_received`; swap_proposals UPDATE → the non-actor gets a state notice (`proposal_countered`/`_accepted`/`swap_confirm_pending`/`swap_disputed`/`proposal_cancelled`), **completed → BOTH parties** `swap_completed` (each row names the OTHER party); messages INSERT → other participant `new_message` (**unread chat notices collapse to one row per conversation** via a partial-unique-index + `ON CONFLICT DO UPDATE`); follows INSERT → `new_follower`; ratings INSERT → `new_rating`. **Owner-only RLS** (read/mark-read), no insert policy (trigger/service-role only); Realtime-published. **Reads + mark-read go DIRECT via RLS — no backend module** (consistent with messages.is_read). Shared: `NOTIFICATION_TYPES` (10), `Notification`/`NotificationWithActor`, DB type, `getNotifications`/`getUnreadNotificationCount`/`markAllNotificationsRead`/`subscribeToNotifications` (INSERT **and** UPDATE). Web: `NotificationBell` (live bell + unread badge + dropdown center, realtime, mark-read-on-open, click-away, renders null logged-out) replacing the `TopBar` stub; `ReviewsList` added to the own `/profile`; bilingual `notifications` namespace. Ran a **5-dimension adversarial-review workflow (14 agents)** → **7 confirmed → 4 distinct fixes**: (1) `swap_completed` actor was `last_actor_id` → showed the 2nd confirmer their own name → now actor = the counterparty per row; (2) realtime was INSERT-only so collapsed message UPDATEs went stale → subscribe to UPDATE too; (3) `new_rating` deep-linked to chat → now links to your `/profile` (received reviews); (4) dropdown a11y (aria-controls/role/labelledby). **2 refuted** (mark-read column-scope mirrors the existing `messages.is_read` policy; null-`conversation_id` fallback unreachable). **Typecheck 8/8, lint clean, build 45/45. 0008 applied to the live DB (transaction-wrapped, additive); structural checks + a full rolled-back functional test of all 5 triggers PASS** (correct routing for both parties, `swap_completed` actor-fix verified, message-collapse=1), and the **RLS write-block confirmed** (an `authenticated` INSERT is rejected by policy via the production-equivalent path).

— Prior: **Ratings (Session 7)** — post-swap reputation, **opt-in/text-optional** (Decision #3: a dismissible prompt after completion; 1–5 stars + optional review). New migration **`0007_ratings.sql`**: `ratings` table (one row per rater per completed swap, `unique(proposal_id, rater_id)`, `check(rater_id<>ratee_id)`, `stars 1..5`), **public-read RLS + admin-manage (writes backend-only)**, new **`profiles.ratings_count`** column, and a **recompute trigger** (`sync_rating_aggregate` → `recompute_user_rating`) that keeps the ratee's `profiles.rating` (avg, 1dp) + `ratings_count` in sync on insert/re-rate/delete — the only writer of those two columns (mirrors `completed_swaps_count`). Mirrored into `full_setup.sql`; `0001` kept in sync. Shared: `Rating`/`RatingWithRater` types, `ratings_count` on Profile/PublicProfile (+ **7** select strings + 2 inline literals updated), `createRatingSchema`, `database.types` table, `getMyRatingForProposal`/`getRatingsForUser` reads, `rateProposal` REST method. Backend: `ProposalsService.rate()` (participant + completed-only guard, ratee derived server-side, upsert) + `POST /proposals/:id/rating`. Web: new `RatingStars` (accessible radiogroup), `SwapRatingPrompt` (opt-in sheet shown post-celebration), `RatingBadge` (profile header + seller mini-card), `ReviewsList` (profile page), extracted shared `Sheet`, completed-card rate/edit UI, bilingual i18n. Ran a **6-dimension adversarial-review workflow (13 agents)** → **5 confirmed (all low) fixed**: (1) celebration auto-dismiss timer restarted on parent re-render → self-stable via onDone-ref + mount-once effect (also hardens Session-6 deal-closing); (2) star radiogroup lacked keyboard nav → roving tabindex + RTL-aware arrow keys; (3) read-only stars announced no value → value-bearing aria; (4) dead Realtime publish on `ratings` (no subscriber) → removed; (5) `SwapRatingPrompt` duplicated the sheet shell → extracted shared `Sheet`. **2 findings refuted** (search_path / RPC-grant — both non-issues: fully-qualified names + SECURITY-INVOKER-under-RLS). **All 8 workspaces typecheck green, web lint clean, web build passes (45/45). 0007 applied to the live DB (session pooler, transaction-wrapped, additive) — 12 structural checks + a rolled-back trigger functional test (4.0/1 → 5.0/1 → update→2.0/1 → delete→null/0) + an RLS test (authenticated INSERT blocked, SELECT allowed) all PASS.**
**Next task (in order):**
1. **Complete the Resend setup above** (5 steps), then test end-to-end: forgot-password → email → reset; and (if "Confirm email" is on) signup → confirmation email → `/auth/confirm` → `/onboarding`. *(Still outstanding — Session 9.)*
2. **Manual two-browser test** of the full lifecycle with `pnpm dev`, both `/ar` and `/en`: propose → counter → accept → both confirm → completed + celebration + each swap-count +1 → **rating prompt → stars/review → profile avg updates**, **+ the notification bell**. **Now also Phase 4:** follow a user → it shows in the **Following** home tab; **block** a user → their listings vanish from your feeds + you can't message them (try an existing chat) + the follow drops on both sides → **Unblock** from `/settings/blocked`; and report a listing from 5 different accounts → it **auto-hides**. (Demo logins: `*@swap.demo`, password `Swap1234!`.) The backend (`apps/api`) must be running for proposal/block mutations. Needs a human — not fully automatable.
   **Now also Phase 5 (admin):** sign in at **`/admin/login`** with the admin demo account → analytics charts render + the date-range picker re-filters them → users table search/filter/pagination → open a **user detail** page → **suspend (duration+reason) / ban / add note / send message** → confirm the suspended user is blocked from acting (and from protected pages) → listings queue **reported/flagged** tabs + feature/request-edits → reports queue **warn/remove/suspend** → **audit log** shows every action (with IP). Non-admins hitting `/admin/login` get rejected.
3. **Phase 6 — finish the remaining items.** **Session 13 landed:** error/loading boundaries · SEO (`generateMetadata`/sitemap/robots/OG) · NestJS Throttler rate limiting · security headers (helmet/CORS + web CSP/HSTS/etc.) · CI GitHub Actions. **Still TODO (next code work):** (a) a full **RTL/LTR + a11y audit** — systematic keyboard/focus/contrast pass (so far only skeleton `aria-hidden` + error-boundary a11y); (b) a **perf pass** — `next/image` audit + bundle analysis (the `/admin` analytics page pulls the Recharts vendor chunk ~120 kB; spec target: no >50 kB first-party chunk); (c) **Playwright E2E** (sign-up → create listing → propose → chat → confirm deal → admin login + a moderation action) — best paired with the human manual QA + a running app; (d) the two deferred Phase-5 lows — web read-level **ban lockout** (RLS/middleware) + **2h admin session timeout** (see Known Issues). Note: the web **CSP is a pragmatic baseline** (`'unsafe-inline'`); tightening `script-src` with a per-request **nonce middleware** is a follow-up.

> **Reality check:** Phases 1–2 real & working; **Phase 3 code-complete + live** (swap loop · ratings · notifications); **Phase 4 code-complete + live** (follow · block · report auto-hide); **Phase 5 code-complete + live** (admin panel: auth · analytics · moderation · audit, Session 12). **Phase 6 IN PROGRESS** — the hardening bundle (error/loading boundaries · SEO · rate-limiting · security headers · CI) is done & green (Session 13, code-only, no DB change); remaining: RTL/a11y/perf audit + Playwright E2E + the 2 deferred lows. Outstanding human-only items: the manual Resend setup (Session 9) + a two-browser manual pass (now covering Phases 3–5).

---

## Session Log

| # | Summary | Ended on |
|---|---|---|
| 1 | Reviewed existing MVP. Auth and basic listing creation are functional. Build plan initialized. | — |
| 2 | Full 7-area parallel audit (api / web-discovery / web-social-chat-admin / web-infra / packages / supabase / mobile+build). Reconciled all phase statuses below. Verdict: Phases 1–2 ~done; chat + realtime work; **entire proposals + notifications layer missing**. Began Phase 3 with the proposals data + API foundation (migration `0005`, `SwapProposal` types, zod schemas, NestJS Proposals module). | 2026-06-18 |
| 3 | Completed the **proposals backend foundation**: migration `0005_proposals.sql` (+ mirrored into `full_setup.sql`), `SwapProposal`/`SwapProposalItem`/`SwapProposalWithRelations` types + `SWAP_PROPOSAL_STATUSES`, zod (`createProposalSchema`/`counterProposalSchema`/`listProposalsQuerySchema`), NestJS `ProposalsModule` (create/counter/accept/decline/cancel/list, turn logic via `last_actor_id`), `PROPOSAL_SELECT`, DB types + REST client. Ran a 3-lens adversarial review → applied fixes: partial-unique dup backstop + 23505 catch, accept-time TOCTOU re-validation, create-failure compensation, conversation back-link error check, idempotent RLS, last_actor WITH CHECK. All typechecks green. **DB migration not yet applied to live Supabase.** | 2026-06-18 |
| 4 | **Removed identity/ID verification entirely** (legal: storing national IDs is restricted in the GCC) — dropped account *and* item verification across 40 files: `verification_requests` table + RLS, `profiles.is_verified`, `listings.is_verified_item`, the NestJS verification module, admin verify actions + verifications queue/page, `verifiedOnly` filter, all verified badges, and the `Verification*` types/schemas/REST methods. **Pivoted trust to completed-swaps + ratings**: added `profiles.completed_swaps_count`, a `SwapCountBadge`, and surfaced it on profiles / seller card / admin users. Updated `AGENT_MASTER_PROMPT.md` §3.9 → "Trust & Reputation". All 8 workspaces typecheck green. **Schema changed → DB must be reset + re-run.** | 2026-06-18 |
| 5 | **Built the swap-proposal UI** (the core barter loop) on the Session-3 backend: `ProposeSwapDrawer` + reusable `ListingPicker` (bundle multi-select, cap, stale-prune) on listing detail; **inbox proposal status badges** (`fetchConversations`→`getProposalStatuses`, `ProposalStatusBadge`); **chat proposal context card** (`ProposalContextCard` — give⇄get, status banner, turn-aware Accept/Counter/Decline/Withdraw, counter picker, Realtime via `subscribeToProposal`); new `@swap/api` read queries (`getProposalByConversationId`/`getProposalStatuses`/`subscribeToProposal`); `ConversationPreview.proposal_status`; full bilingual `proposal` i18n. Ran a 5-dimension adversarial-review workflow (22 agents) → **fixed all 11 confirmed findings**. Added `@swap/validation` to web. Wrote **`supabase/reset.sql`** (grant-safe reset preamble — fixes the latent grant gap in `full_setup.sql`) and **applied `reset.sql` + `full_setup.sql` to the live Supabase DB** (atomically via the session pooler; verified). All 8 workspaces typecheck green; web lint clean. **Live DB now matches the current schema + demo seed.** | 2026-06-19 |
| 6 | **Built Deal Closing** — the final core-loop piece. Migration **`0006_deal_closing.sql`** (+ mirrored into `full_setup.sql`): `swap_confirmations` table (RLS: parties+admin read; backend-only writes), private **`swap-confirmations` bucket** (storage RLS: both parties+admin read via `foldername(name)[1]=proposal_id`), and the atomic **`record_swap_confirmation(p_proposal_id,p_user_id,p_photo_path)` RPC** (row-lock → record → on both-confirmed: `completed` + each party's `completed_swaps_count` +1; `revoke execute` from anon/authenticated, `grant` to service_role only). Backend: `signConfirmationUpload`/`confirm`/`dispute`, `awaiting_confirmation` in CANCELLABLE, all `transition()`s now **status-guarded conditional UPDATEs** (race-safe vs the RPC). Shared: `SwapConfirmation`+`SwapConfirmationView` types, 3 zod schemas, `Database` table+RPC types, `getConfirmations()` (signs private photos), 3 REST methods. Web: `ProposalContextCard` deal-closing actions + confirm/dispute sheets + photo display + `SwapCompleteAnimation` (swap-circle), bilingual i18n, chat-page wiring. **6-dimension adversarial review (16 agents) → fixed 5 distinct confirmed issues** (race guard, RPC EXECUTE lockdown, withdraw-after-confirm, alt text, objectURL leak). Typecheck/lint/build green. **Applied 0006 to the live DB programmatically (transaction-wrapped, additive — no reset/reseed); 9/9 sanity checks passed.** | 2026-06-19 |
| 7 | **Built Ratings** (post-swap reputation) — **Decision #3 resolved: opt-in, text-optional.** Migration **`0007_ratings.sql`**: `ratings` table (one row per rater per completed swap; `unique(proposal_id,rater_id)`, `check(rater_id<>ratee_id)`, `stars 1..5`) + public-read RLS (writes backend-only) + new `profiles.ratings_count` + recompute trigger (`sync_rating_aggregate`→`recompute_user_rating`) maintaining `profiles.rating` (avg, 1dp) + `ratings_count` — the only writer of both (mirrored into `full_setup.sql`; `0001` synced). Shared: `Rating`/`RatingWithRater`, `ratings_count` on Profile/PublicProfile (+ 7 select strings + 2 inline literals), `createRatingSchema`, `database.types`, `getMyRatingForProposal`/`getRatingsForUser`, `rateProposal`. Backend: `ProposalsService.rate()` + `POST /proposals/:id/rating`. Web: `RatingStars` (accessible radiogroup), `SwapRatingPrompt` (opt-in post-celebration sheet), `RatingBadge` (profile header + seller card), `ReviewsList` (profile), extracted shared `Sheet`, bilingual i18n. **6-dim adversarial review (13 agents) → 5 confirmed (all low) fixed** (celebration timer self-stabilised, star keyboard nav, value-bearing aria, dead Realtime publish removed, sheet shell shared) **+ 2 refuted**. Typecheck (8) / lint / build (45/45) green. **0007 applied to the live DB (session pooler, transaction-wrapped, additive); 12 structural + rolled-back trigger functional + RLS checks all passed.** | 2026-06-19 |
| 8 | **Built Notifications** — final Phase-3 item (in-app notification center). Migration **`0008_notifications.sql`**: `notifications` table + **5 SECURITY DEFINER triggers** (proposal INSERT → recipient; proposal UPDATE → non-actor state notice, **completed → both parties, each naming the other**; message INSERT → other participant, **collapsed per conversation** via partial-unique-index + `ON CONFLICT DO UPDATE`; follow INSERT → followed; rating INSERT → ratee). **Owner-only RLS** (read/mark-read; no insert policy → trigger/service-role only); Realtime-published; mirrored into `full_setup.sql`. **Reads + mark-read direct via RLS — no backend module** (mirrors `messages.is_read`). Shared: `NOTIFICATION_TYPES`(10), `Notification`/`NotificationWithActor`, DB type, `getNotifications`/`getUnreadNotificationCount`/`markAllNotificationsRead`/`subscribeToNotifications`(INSERT+UPDATE). Web: `NotificationBell` (live badge + dropdown center, realtime, mark-read-on-open, click-away, RTL/a11y, null-when-logged-out) replacing the `TopBar` stub; `ReviewsList` added to own `/profile`; bilingual `notifications` i18n. **5-dim adversarial review (14 agents) → 7 confirmed → 4 fixes** (swap_completed actor = counterparty per row; subscribe to UPDATE for collapsed msgs; new_rating → /profile; dropdown a11y) **+ 2 refuted**. Typecheck 8/8, lint clean, build 45/45. **0008 applied to the live DB (transaction-wrapped, additive); structural + a full rolled-back functional test of all 5 triggers PASS (routing for both parties, actor-fix, collapse=1); RLS write-block confirmed.** | 2026-06-19 |
| 9 | **Email auth (Resend) + profile onboarding** — Phase-2 gaps. **Email via the Supabase "Send Email" Auth Hook → Resend** (`supabase/functions/send-email` Deno edge function: verifies the hook signature, renders bilingual templates, sends via the Resend API). Web: `forgot-password` / `reset-password` pages, `/auth/confirm` `verifyOtp` callback (open-redirect-guarded; `/auth` excluded from the i18n middleware), register "check your email" + login "Forgot password?" / expired-link notice. **Profile onboarding**: `/onboarding` (name/city/avatar, skippable) + `AvatarUpload` (public `avatars` bucket, fixed key `{userId}/avatar`, **self-persists** `avatar_url`; client MIME/size + **server-side bucket caps** applied to live), avatar on `profile/edit`, `avatar_url` added to `updateProfileSchema`. **5-dim adversarial review (21 agents) → 10 confirmed → 9 fixed** (avatar Skip-loss → self-persist; aria-live on error messages; server bucket caps; MIME-derived fixed key; reset-password transient-session; onboarding logged-out redirect; edge-fn email guard; dead `auth.avatar` key) **+ 6 refuted** (incl. the open-redirect, which was fixed pre-review). Typecheck 8/8, lint clean, build 45/45. **No app DB migration; bucket caps applied to live. ⚠ Manual setup required before emails send — see RESUME.** | 2026-06-19 |
| 10 | **Closed remaining Phase 1-2 functional gaps.** **Listing edit** — new owner-only `/listings/[id]/edit` (fields, condition, category, location, **pause/activate**, **delete**) + `ListingImageManager` (add/remove/**reorder** existing images), backed by new owner-checked endpoints `removeImage`/`reorderImages` (+ `DELETE`/`PATCH :id/images/*`, `reorderImagesSchema`, REST methods); editable only when active/paused (route **and** service guards stop resurrecting completed/removed listings). **Image reorder** in the create uploader (move buttons, pos 0 = cover) + a per-render blob-URL leak fixed. **Listing detail**: `generateMetadata` SEO/Open-Graph + `ShareButton` (native + clipboard). **`/health`** endpoint (unprefixed). **chat-images** read policy → all conversation participants (applied to live). Embedded listing images now **sorted by `sort_order`** in the query layer so reorder is visible on cards/detail/OG. Fixed stale `verifiedOnly` doc (the filter was removed from code in Session 4). **5-dim adversarial review (17 agents) → 9 confirmed → 8 fixed** (status-resurrection route+service guards; Supabase-fallback error surfacing; sort_order ordering; file-input reset; blob-URL identity cache; chat-images comment; delete-error message) **+ 3 refuted**; 1 low (reorder loop non-atomic — recoverable + UI re-syncs) accepted as-is. Typecheck 8/8, lint clean, web build green; no app DB migration (chat-images policy applied live). | 2026-06-19 |
| 11 | **Phase 4 — Trust, Safety & Profiles.** Built the three remaining Phase-4 gaps. **(1) Follow system → two-way:** reusable `FollowButton` (optimistic+revert, server-seeded initial state) on the listing detail + the public profile (`ProfileActions`); a **"Following" feed tab on home** (`FeedTabs`, `?feed=` server-rendered, signed-in only) via `getFollowingListings`/`fetchFollowingListings`. **(2) Block user (greenfield):** migration **`0009`** — `blocks` join table (owner-only RLS), `blocked_between()` helper hiding each party's active listings via the redefined `listings`/`listing_images` read policies; messaging prevented in `proposals.create`/`listings.startConversation`/`conversations.send` **+ the hardened `messages send if participant` RLS** (covers the no-backend direct-send path) **+ the `get_or_create_conversation` RPC**; a `sever_follows_on_block` trigger severs follow edges both ways on every block path; `Block` type + `blocks` Database table + `block/unblock/isBlocked/getBlockedUsers` queries + REST `block/unblock/blockedUsers`; backend `ProfileService.block/unblock/blockedUsers` + `POST/DELETE /users/:id/block` (ParseUUIDPipe) + `GET /me/blocked`; web `ProfileActions`/`UnblockButton` + `/settings/blocked` page. **(3) Report auto-hide:** an `AFTER INSERT` trigger on `reports` flips a listing `active→hidden` at **5 DISTINCT** pending reporters. Bilingual `block` namespace + `home.feed*` keys (removed the dead `chat.block`). Ran a **5-dim adversarial-review workflow (9 agents) → 3 confirmed → 3 fixed** (ParseUUIDPipe on route params; messages-RLS block check; `sever_follows_on_block` trigger replacing the service-only follow delete). Typecheck 8/8, web lint clean, web build green. **0009 applied to the live DB (session pooler, transaction-wrapped, additive); structural + rolled-back functional tests PASS** (`blocked_between` bidirectional/null-safe, follow-sever 2→0, auto-hide active→hidden at the 5th distinct reporter, `authenticated`-role feed read executes the helper). | 2026-06-19 |
| 12 | **Phase 5 — Admin Panel.** Built the full panel on the existing thin admin scaffold. Migration **`0010_admin_moderation.sql`** (additive): `profiles.is_banned` + `suspended_until` + `suspension_reason` + `admin_actions.ip`; AuthGuard rejects banned + actively-suspended (auto-lifts expired temp suspensions); mirrored into `full_setup.sql` + `0001`. **Backend:** audit-`logAction` error-checked + IP-aware (`@Ip()`), self-lockout guard, private **notes** (as `admin_actions` rows → admin-only), **system messages** + **request-edits** (service-role get-or-create conversation), `ParseUUIDPipe` on all routes. **Shared:** `Profile`/`AdminAction` types, widened `adminUpdateUserSchema` + note/message schemas, REST `addUserNote/messageUser/requestListingEdits`. **Web:** `lib/admin-analytics.ts` (5 cards + sparklines + daily-line/weekly-bar/category-donut/city-hbar/funnel, date-range-aware), rewritten `lib/admin.ts` (Paged users/listings/reports/audit + `fetchAdminUserDetail` + `effectiveSuspended`), Recharts `charts.tsx`, `DateRangePicker`, `url-controls`, `action-kit` (dialogs w/ error surfacing), `UserActions`/`ListingActions`/`ReportActions`, `(panel)` route-group restructure + `(panel)/{page,users,users/[id],listings,reports,audit}` + `/admin/login` (outside the guard), sidebar Audit Log, Sheet a11y, full bilingual `admin` namespace (parity 513=513). Added **recharts**. Ran a **6-dim adversarial-review workflow (26 agents) → 19 confirmed (1 refuted; migration agent died → self-reviewed) → 16 fixed + 2 lows deferred** (dup-conversation race; 2h session timeout): audit-error swallowing, unchecked participant insert, self-lockout, sparkline-upper-bound, reports 500-cap blank pages, dialog/escalation error+refresh, web ban-gate in `requireUser`, effective-suspension badges, localized proposal-status/reasons/pagination, actor-IP, Warn-user. Typecheck 8/8, web lint clean, web build green. **`0010` applied to the live DB (session pooler, transaction-wrapped, additive); 4/4 columns verified + `authenticated`-role read PASS.** | 2026-06-19 |
| 13 | **Phase 6 — production-hardening bundle (code-only, no DB change).** Error boundaries (`global-error` + catch-all `[locale]/error` + 5 per-route, all over a shared `RouteError`), 10 `loading.tsx` skeletons (+ shared `ProfileListingsSkeleton`, `aria-hidden`), SEO (`robots.ts` + build-safe `sitemap.ts` + `metadataBase` / locale-aware `generateMetadata` w/ OG on the root layout + profile/categories/listings), NestJS **Throttler** (global 120/min + 30/min on the 2 upload-sign endpoints, `@SkipThrottle` on `/health`, env-gated `trust proxy`), **security headers** (`helmet` + CORS allowlist on the API; web prod-only CSP + HSTS/X-Frame-Options/nosniff/Referrer-Policy/Permissions-Policy), and **CI** (GitHub Actions → `turbo typecheck/lint/build`). 5-dim adversarial review (11 agents) → **6 confirmed (0 refuted) → all 6 fixed** (global-error logs `error` + tags its English line `lang=en/dir=ltr`; CORS empty-string `\|\|` guard; env-gated trust-proxy; skeleton `aria-hidden`; `ProfileListingsSkeleton` dedup). Typecheck 8/8, web lint clean, web build green. No migration. | 2026-06-20 |
| 14 | **Design overhaul — warm-dark theme (design/CSS only, no logic/DB change).** Replaced the navy/green light theme with a single warm-dark "workshop at night" system: terracotta accent (#E8572A) on warm near-black surfaces (base #0F0E0D / surface #1C1A18 / elevated #252320). Wired tokens into `tailwind.config.ts` (legacy semantic names — canvas/ink/muted/line/navy/green/swap — **remapped** to dark so existing utility classes flip with near-zero churn) + synced `packages/config/src/theme.ts`; rewrote `globals.css` (dark `:root`, `color-scheme:dark`, dark `.card/.btn/.input/.chip`, dark skeleton shimmer, accent focus/selection, dark scrollbars, `float`/`swap-spin` keyframes). **Type:** Cairo → **IBM Plex Sans Arabic** (ar) + **Inter** (en) via `next/font`, per-locale body face. **Motion:** added **framer-motion** + `components/motion.tsx` (`Reveal`/`Stagger`/`StaggerItem`/`RotatingSwap`), all `prefers-reduced-motion`-gated. **Migration:** scripted `text-navy→text-ink` + solid `bg-white→bg-surface` across 41 files; status/amber/red/gray literals → dark tints; global chrome (TopBar/Footer/MobileBottomNav/cards) darkened; theme-aware `Logo` (the baked dark-navy wordmark PNG → live text so it reads on dark). **Pages:** landing (dark hero, staggered entrance, living swap-circle, floating card, scroll reveals, premium CTA); split-screen auth via new `AuthShell` (brand panel + full-width buttons + grouped signup); product detail (480px contained gallery, clarified give⇄want, prominent grouped actions); **orphan-proof `ListingGrid`** (auto-fill minmax); card hover-lift; dark admin (accent sidebar, dark Recharts palette + dark tooltips/legend, dark controls/tables). Ran a **5-dim review workflow (RTL/motion/contrast/coherence/logic-safety)** → logic-safety **clean** (confirmed CSS-only); applied valid fixes (`animate-spin` added to the reduced-motion kill-switch, `placeholder`/empty-icon `text-faint→text-muted`, 4 undefined `text-ink-muted→text-muted`, card label `text-green-dark→text-accent`); rejected false-positive "contrast" flags (bright pastel text on dark tints is ~9–12:1) + intentional dark-glass overlays. **Then, per user feedback, re-themed from terracotta to the brand colors: deep-navy dark surfaces (night #0A0E1A / surface #121829 / elevated #1B2438) + brand green accent (#18B66A) + brand navy panels (#0B1324, sidebar/hero), cool off-white text (#E9EDF6).** All accent/surface hexes swapped in `tailwind.config.ts`+`theme.ts`+`globals.css` + the inline gradients (Hero/AuthShell/CTA), Recharts palette, admin sidebar, manifest, global-error. **Also renamed the darkest color token `base`→`night`** (a `base` color collides with Tailwind's built-in `text-base` font-size utility — was bleeding the dark color onto every `text-base` element). Logo wordmark now Just(green)+Swap(off-white); the green brand mark is now on-palette. **Web build green, workspace typecheck 8/8, web lint clean.** | 2026-06-20 |
| 15 | **User-switchable light + dark themes.** Re-architected the palette as CSS variables (space-separated RGB channels → `rgb(var(--token) / <alpha-value>)`) so every semantic class (bg-surface/text-ink/border-line/bg-accent…) flips by theme with no per-file `dark:` churn and all opacity modifiers keep working. `globals.css` defines a light `:root` (navy/green on #F5F7F8/white) + a `.dark` override (deep-navy #0A0E1A/#121829); shadows, skeleton shimmer, scrollbars, selection and `<img>` bg are var-driven too. Added **next-themes** (`class` strategy, `defaultTheme=dark`, persisted, `suppressHydrationWarning`, `disableTransitionOnChange`) via a `ThemeProvider` wrapper; a mount-safe **`ThemeToggle`** (sun/moon, bilingual aria-label) in the TopBar + admin sidebar + admin mobile bar. Handled dark-only assumptions: an always-light **`onnavy`** token for the navy panels (AdminSidebar, hero side-panel, FlowCard — navy is dark in BOTH themes); theme-aware **`.bg-brand`/`.bg-cta`** gradient utilities (hero/auth/CTA); **status badges** given light defaults + `dark:` overrides (badges/safety/action-kit/reports/listings/user-detail/edit/primitives/proposal-card); **admin charts** axis/grid/tooltip now adapt via `useTheme()` (`darkMode:'class'`). `theme.ts`/legacy tokens (canvas/ink/muted/line/green/swap/navy) all resolve through the vars. **Web build green, workspace typecheck 8/8, web lint clean.** | 2026-06-20 |

---

## Phase Checklist

Legend: `[x]` done · `[~]` partial / present but incomplete · `[ ]` not started · `[!]` blocked

**Why a `[~]` is still `[~]` — every one carries a tag:** `(deliberate)` = done differently from the spec on purpose (won't change without a decision) · `(by design)` = a resolved architecture choice, working as intended · `(Decision #N)` = settled in Open Decisions · `(Phase N)` = the remaining work belongs to that later phase, not to Phases 1–3 · `(deferred — low ROI)` = completable but intentionally postponed. **None of the `[~]` below are dropped/forgotten Phase 1–3 work.**

---

### Phase 1 — Foundation
> Infrastructure, packages, DB schema, all apps scaffold and connected.

- [~] **(by design)** Turborepo pipeline — workspace + `turbo.json` + root scripts are real, but coverage is uneven: only `web`/`api` have build/dev; packages have only typecheck+lint; mobile excluded from `turbo run dev`; api `build` is `tsc --noEmit` (emits no `dist`).
- [~] **(deferred — low ROI)** `packages/config` — theme tokens ✓, GCC countries/cities ✓, bilingual safety copy ✓. **Diverges:** 26 top-level categories (covers the 8 spec concepts) but only ~4 demonstrative subcategories total, not 4–6 per category.
- [x] `packages/types` — all domain types present incl. `SwapProposal`/`SwapProposalItem`/`SwapConfirmation`/`SwapConfirmationView` (Sessions 3, 6), `Rating`/`RatingWithRater` (Session 7), `Notification`/`NotificationWithActor` + `NOTIFICATION_TYPES` (Session 8), `Block` (Session 11). `profiles.completed_swaps_count` + `ratings_count` are the trust signals; `Verification*` removed (Session 4).
- [x] `packages/validation` — zod for profile/listings/messages/reports/admin/catalog + proposal + deal-closing + `createRatingSchema`. Notifications + blocks need **no** input schema (notifications: DB triggers + direct-RLS reads; blocks: target id is a uuid route param — `ParseUUIDPipe` validated — and follow-severing is a DB trigger).
- [~] **(`tsvector` deferred)** DB schema — `swap_proposals`/`swap_proposal_items` (0005), `swap_confirmations` + RPC (0006), `ratings` + recompute trigger (0007), `notifications` + 5 trigger fns (0008), `blocks` + `blocked_between()` + `sever_follows_on_block` + `auto_hide_reported_listing` triggers (0009), **moderation columns `profiles.is_banned/suspended_until/suspension_reason` + `admin_actions.ip` (0010)** now exist. No `tsvector`/GIN full-text search (uses ILIKE).
- [x] `supabase/migrations/0002_rls.sql` — RLS enabled + ≥1 policy on all 15 tables.
- [x] Storage buckets — `avatars`, `listing-images`, `chat-images` (0003) + `swap-confirmations` (0006). **Server-side caps** (5 MB + image MIME) on the image buckets (Session 9). **`chat-images` READ now allows all conversation participants** (Session 10, applied to live), not just the uploader.
- [x] `packages/api` — Supabase client factory + real typed query fns (listings/profiles/chat/social) + full typed REST client to NestJS + hand-written `Database` type for all 15 tables. Far beyond stubs.
- [x] `apps/api` — Real NestJS 10: `api/v1` prefix, CORS, Swagger at `/api/docs` (supabase-jwt), JWT `AuthGuard` + `AdminGuard`, Zod pipe, modules. **Added (Session 10): `/health` liveness endpoint** (unprefixed). *(Auth is a per-route guard, not global middleware — fine for this design.)*
- [~] **(deliberate)** `apps/web` — Next.js 14 App Router + TS ✓, next-intl ar(default,RTL)/en(LTR) with `dir`/`lang` in root layout ✓, live Supabase data layer ✓. **Diverges:** navy/green palette + Cairo font instead of the spec's terracotta/sand/ink CSS-variable token system; `globals.css` only defines `--background`/`--foreground`.
- [~] **(Phase 7)** `apps/mobile` — Valid Expo Router (SDK 51) scaffold consuming `@swap/*` via Metro. **Missing:** the "basic tab navigator" — `_layout.tsx` is a single Stack with one `index` route.

---

### Phase 2 — Core Listing & Discovery
> Users can sign up, create listings, and browse.

- [x] Auth flow (web): sign up + login — work in RTL + LTR.
- [x] Auth flow: **email verification + forgot/reset password** (Session 9) via **Resend** (Supabase "Send Email" Auth Hook → `supabase/functions/send-email` edge function, bilingual templates). `forgot-password` + `reset-password` pages, `/auth/confirm` `verifyOtp` callback (open-redirect-guarded; `/auth` excluded from i18n middleware), register "check your email" handling, login "Forgot password?" + expired-link notice. *(Passwordless OTP-login `signInWithOtp` not added — out of scope; email-confirm + recovery cover "verify email + forgot password".)*
- [x] Profile setup (Session 9): dedicated **`/onboarding`** route (name/city/avatar, skippable) reached after signup/confirm + **avatar capture/upload** (`AvatarUpload` → public `avatars` bucket, fixed key, self-persists `avatar_url`); avatar also on `profile/edit`. `avatar_url` added to `updateProfileSchema`.
- [x] NestJS Listings module: list (filters: search/category/country/city/condition + sort + `.range` pagination), get, create, update (ownership-checked), soft-delete, **+ image add/remove/reorder (Session 10, owner-checked)**. (The old `verifiedOnly` filter was removed with verification in Session 4 — no longer in the code.)
- [~] **(Decision #6 / by design)** NestJS Storage module: **no dedicated module** — listing-image signed-upload logic is embedded in listings.service. **Improved (Session 9):** avatar upload added (direct to the public `avatars` bucket, RLS owner-write) with **client MIME/size validation + server-side bucket caps** (`file_size_limit` 5 MB + `allowed_mime_types` image-only on avatars/listing-images/chat-images, applied to the live DB). **Still:** listing image limit `FREE_PLAN_MAX_IMAGES=4`; no chat-image signing.
- [x] Listing create/edit (Session 10): 3-step create wizard + **image reorder** (move-earlier/later, position 0 = cover) + a full **edit route** `/listings/[id]/edit` (fields, condition, category, location, **status pause/activate**, **delete**, and add/remove/reorder existing images via the new backend endpoints). Owner "Edit" link on the detail. **Still free-text:** the "open to swap for" side (no category picker) — minor.
- [x] Listing detail page (Session 10): SSR + all fields + thumbnail gallery + **`generateMetadata` per-listing SEO/Open-Graph** + **Share button** (native share + clipboard fallback). **Still:** swipeable gallery (click-thumbnail only) — minor polish.
- [x] Home feed: server-rendered featured + newest grids, Hero, category rail, `EmptyState`; route-level skeleton (`listings/loading.tsx`).
- [x] Explore page: category grid → `/listings?category={slug}` with localized names + icons.
- [x] Search: `SearchBar` in nav → `/listings?search=`; URL-driven `ListingFilters` (category/country/city/condition/sort). ILIKE search (no `tsvector` yet — deferred).
- [x] Listing card: logical props, localized names, embedded `SwapPair`, artwork fallback, responsive grid, matching skeleton variant.
- [x] Saved listings: optimistic `SaveButton` (API-first + Supabase fallback, revert-on-error, login redirect) + SSR saved page.
- [x] `supabase/seed.sql`: exactly 12 users (1 admin, 6 verified) + 44 listings across many cities/countries + 23 categories (88 image rows). ⚠ Hard-depends on migration `0004` running first.

**Exit criterion:** ✅ Met — register → create listing with photos → another user finds & views it.

---

### Phase 3 — Proposals & Chat   ← **current focus**
> The full exchange loop works end-to-end.

- [x] NestJS Proposals module: create / counter / accept / decline / cancel / list-by-user, with `last_actor_id` turn logic, ownership + active checks, dup backstop, accept-time re-validation. Built + adversarially reviewed (Session #3). *(Backend only — UI is the next item; live DB migration still pending.)*
- [x] NestJS Conversations module: list + get-with-messages + send + **proposal-triggered + linked** (`conversations.proposal_id`, set by `ProposalsService.create`) + Realtime. The standalone chat-"start" path (`POST /listings/:id/start-conversation`) is an intentional addition for message-only contact (message-before-proposing).
- [x] NestJS Messages module: `send()` (participant-checked, body + image_url, bumps `updated_at`) + ordered `messages()` list. Folded into the conversations module.
- [x] Supabase Realtime: `subscribeToMessages` on `postgres_changes` INSERT filtered by `conversation_id`; wired in `ChatRoom.tsx` with id-dedup.
- [x] "Propose a Swap" drawer (Session 5): `ProposeSwapDrawer` on listing detail replaces the old "Interested" button — reusable `ListingPicker` (multi-select bundle, `MAX_PROPOSAL_ITEMS` cap, exclude-self) + safety disclaimer + note → `api.createProposal` → navigates to the conversation. "Message" still calls `startChat()`; actions hidden for the owner (`isOwner`).
- [x] Inbox page: conversation list with **real unread counts** (green pill) **+ proposal status badges** (Session 5) — `fetchConversations` batch-resolves `conversations.proposal_id → swap_proposals.status` (`getProposalStatuses`, degrades gracefully) rendered via `ProposalStatusBadge`.
- [x] Chat view: real-time bubbles ✓, compact safety disclaimer ✓, optimistic composer ✓ **+ proposal context card & status banner** (Session 5) — `ProposalContextCard` (SSR-fetched via `getProposalByConversationId`) shows give⇄get, status, banner, and turn-aware actions; Realtime-synced via `subscribeToProposal`.
- [x] Counter-proposal flow (Session 5): "Counter" re-opens the picker on the **proposer's** listings (`ownerId=proposer_id`), pre-seeds current offered items, prunes stale/inactive ids via `onLoaded` → `api.counterProposal`.
- [x] Deal Closing flow (Session 6): `agreed → awaiting_confirmation → completed | disputed`. Each party uploads a photo of the item they received (private `swap-confirmations` bucket, both-parties+admin read); the atomic `record_swap_confirmation()` RPC completes the swap once both confirm and increments **both parties' `completed_swaps_count` exactly once** (the only place that counter changes). `ProposalContextCard` "We've exchanged" / "Something went wrong" / Withdraw actions, received-photo display, `SwapCompleteAnimation` swap-circle on completion. Dispute → admin-visible report. Race-safe (status-guarded transitions) + RPC locked to service_role. **Remaining:** the post-deal **rating prompt** is deferred to the Ratings task below.
- [x] Rating / Review (Session 7): **opt-in, text-optional** (Decision #3). `ratings` table + recompute trigger maintaining `profiles.rating` (avg, 1dp) + new `profiles.ratings_count` (the only writer of both). `Rating`/`RatingWithRater` types, `createRatingSchema`, `getMyRatingForProposal`/`getRatingsForUser` reads + `rateProposal`; `ProposalsService.rate()` (participant + completed-only guard, ratee derived server-side, upsert) + `POST /proposals/:id/rating`; post-completion `SwapRatingPrompt` (+ rate/edit on the completed card), `RatingBadge` (profile header + seller mini-card), `ReviewsList` (profile page). 6-dim adversarial review (13 agents) → 5 low findings fixed. Typecheck/lint/build green; **0007 applied to live DB + verified (structural + trigger functional + RLS).**
- [x] Notification triggers (Session 8): `notifications` table + 5 SECURITY DEFINER triggers (0008) create a notice on new proposal, every proposal state change (incl. **deal closed** → both parties, **new rating**), new message (collapsed per conversation), and new follower. The trigger-based design fires regardless of write path (backend / direct RLS / completion RPC).
- [x] In-app notification center (Session 8): `NotificationBell` replaces the stub — live unread badge, dropdown center with per-type localized text + actor avatar + relative time + deep-link, Realtime (INSERT+UPDATE), mark-read-on-open, click-away, RTL/a11y, renders null when logged-out. Reads + mark-read go direct via owner-only RLS.

**Exit criterion:** ✅ **MET (code-complete; pending a two-browser manual pass).** Full swap lifecycle — propose, counter, agree, confirm (both upload), close, **rate** — with real-time chat **and live in-app notifications** throughout. All Phase-3 items built + live-DB-applied.

---

### Phase 4 — Trust, Safety & Profiles

- [x] User profile pages: public `users/[username]` (header + listings + **ratings** + report) + own `profile` + working `profile/edit` form (text fields). Session 7 added the **avg-rating `RatingBadge`** (header + seller mini-card) and a **`ReviewsList`** section on the public profile. (Avatar editing still absent.)
- [x] Follow system (Session 11): **two-way** — reusable `FollowButton` (optimistic + revert, initial state seeded server-side) used on the listing detail (replacing the old one-way toggle) **and on the public profile** (via `ProfileActions`); a **"Following" feed tab on home** (`FeedTabs`, server-rendered via `?feed=following`, signed-in only) backed by `getFollowingListings` / `fetchFollowingListings`. `unfollow`/`isFollowing` now wired through the web.
- [x] ~~Verification request flow~~ **REMOVED (Session 4)** — Swap does not verify identity (storing national IDs is legally restricted in the GCC). Replaced by the **completed-swaps trust signal** (`profiles.completed_swaps_count`, rendered via `SwapCountBadge` on profiles + seller card + admin users). The counter is incremented by the Deal-Closing flow (Phase 3), never by an admin.
- [x] NestJS Reports module: inserts reports (reporter/target/reason/status); returns **202** (spec §3.8) — both `POST /reports` and `POST /listings/:id/report`; the REST client handles the empty 202 body.
- [x] Report UI: working `ReportDialog` modal wired on listing detail (`listing`) and public profile (`user`).
- [x] Auto-flag at threshold (Session 11): an `AFTER INSERT` trigger on `reports` (`auto_hide_reported_listing`, migration 0009) flips a listing `active→hidden` once **`REPORT_AUTO_HIDE_THRESHOLD` (=5) DISTINCT** users file pending `listing` reports. A DB trigger (not app code) so it covers every report write path; distinct-reporter count so one spammer can't unilaterally hide. The feeds already exclude non-active listings, so this is a complete soft-hide. (See Open Decision #10.)
- [~] **(Phase 4 — mostly done)** Safety copy banner: `SafetyDisclaimer` shown on listing detail, chat, and safety page — but there's no proposal-confirmation screen yet (its spec'd location).
- [x] Block user (Session 11): `blocks` table + owner-only RLS + `blocked_between()` helper (migration 0009). **Hides each party's active listings from the other** via the redefined `listings`/`listing_images` read policies (the web reads listings direct via RLS, so this covers all feeds). **Prevents messaging** via `assertNotBlocked` in `proposals.create`/`listings.startConversation`/`conversations.send`, the hardened `messages send if participant` RLS (covers the no-backend direct-send path), and the `get_or_create_conversation` RPC guard. **Severs follow edges both ways** via the `sever_follows_on_block` trigger. UI: `ProfileActions` block/unblock on the public profile + a `/settings/blocked` management page (`UnblockButton`). Backend `POST/DELETE /users/:id/block` + `GET /me/blocked`.

**Exit criterion:** ✅ **MET (code-complete; pending the shared two-browser manual pass).** Follow system works two-way + Following feed; reports auto-hide flagged listings (5 distinct reporters) which are then excluded from every feed; block hides listings + prevents messaging. 0009 applied + verified on the live DB.

---

### Phase 5 — Admin Panel

- [x] Admin auth (Session 12): separate **`/admin/login`** page (own form; signs in → verifies `is_admin` → redirects `/admin`, signs non-admins back out) placed **outside** the guarded `(panel)` route group so the `requireAdmin` layout guard never blocks the login screen. `AdminGuard` on the API unchanged. *(2-hour session timeout from spec §4.1 still open — see Known Issues; it's a Supabase session-lifetime config.)*
- [x] Admin layout (Session 12): sidebar now includes **Audit Log** (Dashboard, Users, Listings, Reports, **Audit Log**, Categories, Countries, Cities). Restructured into `admin/(panel)/*` (guard + sidebar) with `admin/login` outside it.
- [x] Analytics dashboard (Session 12): **5 spec cards** (Total users, Active listings, Completed swaps, Open proposals, Pending reports) each with a 30-day **Sparkline**, plus the 5 **Recharts** charts — daily new users+listings (line), completions/week (bar), category (donut), city (horizontal bar), proposal funnel — and a **`DateRangePicker`** (7/30/90 + custom) wired to all charts via `?from&to`. Computed in `lib/admin-analytics.ts` (admin RLS reads, JS bucketing; funnel uses current status as a "reached" proxy — documented).
- [x] Users table (Session 12): server-rendered, **paginated + searchable (name/@username/email) + status-filtered** (all/active/suspended/banned/admin); row actions **suspend (duration + reason) / unsuspend / ban / unban**; effective-suspension aware (expired temp suspensions don't render as Suspended). `UserActions` → backend API (audit-logged).
- [x] User detail page (Session 12): `admin/users/[id]` — profile facts, listing history, swap history, report history (against the user **or** their listings), reviews received, and **admin notes / action history** (audit rows targeting the user). Detail-variant actions add **private note** + **system message**.
- [x] Listings queue (Session 12): **filter tabs (all / reported / flagged)** + title search + pagination; owner + pending-report-count columns; actions **approve / hide / show / feature / unfeature / request-edits (messages owner) / remove**.
- [x] Reports queue (Session 12): **severity-sorted (scam/dispute > inappropriate > spam > other) + age**, sort toggle, expandable cards (reporter, resolved target label, description); actions **resolve / dismiss / warn user / remove listing / suspend user** (escalations chain two audit-logged calls + always refresh).
- [x] ~~Verifications queue~~ **REMOVED (Session 4)** — verification feature dropped; page + endpoints + `verification_requests` table deleted.
- [x] Audit log page (Session 12): `admin/audit` — searchable (action/target/notes), paginated; resolves admin usernames + links user/listing targets. Backed by `fetchAdminActions`.
- [x] All admin writes via `/admin/*` NestJS endpoints, each logged to `admin_actions` via `logAction()` — now **error-checked** (audit failure throws, never silently lost) and **records actor IP** (`@Ip()` → new `admin_actions.ip` column, spec §4.1). Self-lockout guard: an admin can't flip their own admin/ban/suspension state.

**Exit criterion:** ✅ **MET (code-complete; pending a manual admin pass).** Separate `/admin/login`, analytics (Recharts + date-range), moderation actions (suspend/ban/warn/feature/remove/notes/messages) all audit-logged with actor IP, user-detail + audit-log pages. 0010 applied + verified on the live DB. *(Residual: full web read-level lockout of banned users + 2h session timeout are Phase-6 hardening — see Known Issues.)*

---

### Phase 6 — Polish & Web Launch

- [~] **(Phase 6 — audit pending)** Full RTL/LTR: correct at layout level (dir per locale) + logical props; new error/loading boundaries are bilingual (`states`/`nav` i18n; `global-error` tags its EN line `lang=en/dir=ltr`). Still no comprehensive page-by-page audit; a few hardcoded strings (not-found, language switcher); `:focus-visible` ring on only one component.
- [~] **(Phase 6 — audit pending)** Accessibility: Session 13 added `aria-hidden` on all skeleton roots + accessible error boundaries (the `ErrorState` retry button). Still no systematic keyboard/focus/contrast pass.
- [x] SEO (Session 13): `robots.ts` (disallows admin/messages/settings/etc.), build-safe `sitemap.ts` (`force-dynamic` + guarded Supabase fetch → static routes ×2 locales + active listings), `metadataBase` + locale-aware `generateMetadata` (title template, OG/Twitter/`alternates`) on the root layout, plus per-page `generateMetadata` on listing detail (pre-existing), profile, categories, and listings (search/category-aware).
- [~] **(Phase 6)** Performance: `next/image` used (fill+sizes, priority on detail) + force-dynamic SSR; no bundle analysis / perf pass yet (the `/admin` analytics page pulls the Recharts vendor chunk ~120 kB).
- [x] Error boundaries (Session 13): `global-error.tsx` (self-contained bilingual, logs `error`) + a catch-all `[locale]/error.tsx` + 5 per-route `error.tsx` (listings, listings/[id], chat, user profile, admin panel), all over a shared `RouteError` (reuses the `ErrorState` primitive + `states` i18n, `reset()` + Home link).
- [x] Loading states (Session 13): 10 new `loading.tsx` — a neutral catch-all `[locale]/loading.tsx` + tailored skeletons for listing detail, inbox, chat, categories, saved, my-listings, admin panel, and a shared `ProfileListingsSkeleton` for profile + public profile (on top of the existing `/listings`). All `aria-hidden`.
- [x] Rate limiting (Session 13): `@nestjs/throttler` added; `ThrottlerModule.forRoot([{ttl:60s, limit:120}])` + global `APP_GUARD ThrottlerGuard`; stricter `@Throttle` (30/min) on the two upload-signing endpoints (`listings signImage`, `proposals signConfirmation`); `@SkipThrottle` on `/health`; env-gated `trust proxy` (`TRUST_PROXY`) so it keys on the real client IP behind a proxy. *(Auth isn't in NestJS — Supabase handles it — so nothing to throttle there.)*
- [x] Security headers (Session 13): API — `helmet` (CSP off since it serves JSON + Swagger; CORP cross-origin) + a **CORS allowlist** (`CORS_ORIGINS`/`NEXT_PUBLIC_APP_URL`) replacing `cors:true`. Web — `next.config.mjs` `headers()`: always-on X-Content-Type-Options/X-Frame-Options=DENY/Referrer-Policy/Permissions-Policy/HSTS, plus a **prod-only CSP** (dev HMR preserved). CSP is a pragmatic `'unsafe-inline'` baseline — nonce-based `script-src` is a documented follow-up.
- [ ] E2E tests: no Playwright / test suites. *(Best paired with the human manual-QA pass + a running app.)*
- [x] CI pipeline (Session 13): `.github/workflows/ci.yml` — pnpm (from `packageManager`) + Node 20 → `pnpm install --frozen-lockfile` → `turbo typecheck` / `lint` / `build`, on PR + push to main, with concurrency cancel-in-progress.

**Exit criterion:** Lighthouse ≥ 90 on all four categories on listing detail page. All E2E tests pass.

---

### Phase 7 — Mobile Applications
> **Detailed multi-session tracker: `docs/mobile-release-plan.md`** (phases M0–M7 + store readiness/compliance). This is the summary — keep the two in sync.

- [x] **M0 — toolchain & live-data baseline (Sessions 2–3, 2026-07-22):** upgraded to **Expo SDK 57** (RN 0.86 / React 19.2.3, New Arch on); mobile now reaches the **live backend** via shared `@swap/api` — Supabase auth with **AsyncStorage session persistence** (fixed `createSupabaseClient` to accept a storage adapter — additive, web unaffected), a wired `SwapApiClient` (`getToken`), RLS reads, Realtime. Bundle id `me.justswap.app`, name "JustSwap", scheme `justswap`. Typecheck 8/8 green.
- [~] **(M0 — device smoke BLOCKED)** Dev runtime = a **local Android dev build** (`expo run:android` + `expo-dev-client`), not Expo Go / EAS. Toolchain (NDK 27 / CMake 3.22 / platform-36), the Pixel_3a API-34 emulator, and the local NestJS API are set up; **`nodeLinker: hoisted`** fixed a pnpm path-length build failure and all third-party native modules compile. **Blocked** at the New-Arch codegen 260-char path limit → needs Windows `LongPathsEnabled` (elevated registry write, denied to the agent). See the mobile plan's Known gaps for the command.
- [~] **(Phase 7)** Shared API from `packages/api`: now **used** (M0 wires the Supabase client + `SwapApiClient`). Feature screens (M2–M4) consume the existing query/REST functions.
- [ ] Custom RN components (ListingCard, CategoryGrid, ChatBubble, ProposalBanner, BottomSheet, ImageGallery): none yet — M1. Home is a temporary M0 connectivity harness.
- [ ] Navigation: tabs + nested stacks — none yet (M1). i18n/RTL — M1.
- [ ] Camera / push / biometrics / deep links: none yet (M5). Universal-Links/App-Links domain `justswap.me`.
- [ ] Store assets + compliance (Apple 1.2 UGC report/block/filter, privacy nutrition labels, Play Data Safety, content rating, screenshots): none yet — M4 (safety) + Phase S (store/accounts).
- [ ] EAS Build/Submit (`eas.json`): none yet (M6).

**Exit criterion:** Both apps installable on device; full swap lifecycle functional on mobile. *(M0 nearly closes the Android half — one admin action, Windows long paths, away from a running build; details in `docs/mobile-release-plan.md`.)*

---

## Open Decisions

| # | Question | Status |
|---|---|---|
| 1 | Admin panel as `/admin` route group, or separate `apps/admin`? | ✅ **Resolved** — implemented as `/[locale]/admin/*` route group inside `apps/web` (already built). Keep it there. |
| 2 | Deal-closing confirmation photo — visible to both parties, or private confirmation signal? | ✅ **Resolved (Session 6): both parties + admins.** Each party sees the other's confirmation photo (max trust/accountability + dispute evidence). Implemented via the private `swap-confirmations` bucket with a storage SELECT policy keyed on `proposal_id` for proposer/recipient/admin; the web signs each path on read. |
| 3 | Ratings after every swap (mandatory) or opt-in? | ✅ **Resolved (Session 7): opt-in, text-optional.** A dismissible prompt appears once right after the completion celebration ("Maybe later"); 1–5 stars required to submit, review text optional. Reachable later via "Rate this swap" / "Edit" on the completed proposal card. Never blocks. Matches the spec wording ("1–5 stars + optional text"); maximises goodwill over coverage. |
| 4 | Proposal bundles — can a proposer offer multiple of their listings in one proposal? | ✅ **Resolved** — yes. Spec says "(or bundle)", so the data model uses a `swap_proposal_items` join table (target listing is single; offered items are 1..n). |
| 5 | Decline vs cancel — separate statuses? | ✅ **Resolved** — keep the spec's literal enum (`pending → countered → agreed → awaiting_confirmation → completed | disputed | cancelled`). Both "decline" (by recipient) and "cancel" (by proposer) resolve to `cancelled`. |
| 6 | Listing image limit — spec says 8, code enforces 4 (`FREE_PLAN_MAX_IMAGES`). Align up to 8 or keep plan-gated? | ✅ **Resolved (Session 6): keep 4, plan-gated.** Free stays at 4; 8/15 remains the future premium tier (`PREMIUM_PLAN_MAX_IMAGES`). Diverges from the spec's literal 8 until payments ship, preserving the upsell path. (Independent of Deal Closing — confirmation photos are 1 per party.) |
| 7 | Counter semantics — when User B counters, the picker shows **User A's (the original proposer's) listings**, so B re-selects which of A's items are on the table; the API validates offered items against `proposer_id`. | ✅ **Resolved per spec §3.4** ("Counter re-opens the listing picker"). B is never bound — A must still `accept`. Revisit only if product wants symmetric two-sided offers. |
| 8 | Identity / item verification — keep, or remove? | ✅ **Resolved (Session 4): removed entirely.** Collecting/storing national IDs is legally restricted in the GCC and not reliably verifiable. **No "verified account" or "verified item" badge.** Trust = **completed-swaps count** (`profiles.completed_swaps_count`, +1 per side on undisputed completion) + ratings. `is_verified`, `is_verified_item`, the `verification_requests` table, and the whole verification module/UI were deleted. |
| 9 | Notifications — backend NestJS module, or DB-trigger-driven + direct RLS? | ✅ **Resolved (Session 8): trigger-driven, no module.** 5 SECURITY DEFINER triggers create notices (fires on every write path incl. the completion RPC); reads + mark-read go direct via owner-only RLS — consistent with how `messages.is_read` and inbox unread-counts already work. A NestJS module would only duplicate direct reads. The spec's "notifications module" is satisfied by the table+triggers+RLS contract. |
| 10 | Report auto-hide threshold — runtime env var `REPORT_AUTO_HIDE_THRESHOLD`, or a DB trigger? And total reports or distinct reporters? | ✅ **Resolved (Session 11): DB trigger, distinct reporters, constant=5.** A DB `AFTER INSERT` trigger (`auto_hide_reported_listing`) is the single chokepoint that covers EVERY report write path (backend service-role, the direct-RLS web fallback, the proposal-dispute insert, seed) — the spec's env var can't reach a trigger, so the threshold is a documented constant (mirrors `REPORT_AUTO_HIDE_THRESHOLD`, kept in `.env.example` for discoverability; retune by editing the constant). It counts **DISTINCT `reporter_id`** so one user can't unilaterally hide a listing. Scope = `target_type='listing'` only (auto-suspending a user account is heavier → left to manual moderation). Same trigger-over-app-code rationale as Decision #9. |

---

## Known Issues / Blockers

- [RESOLVED] ~~Listings `verifiedOnly` filter corrupts pagination~~ — moot: the `verifiedOnly` filter was removed entirely with verification (Session 4) — added 2026-06-18, resolved 2026-06-18
- [GAP] Storage: image limit kept at 4 (Decision #6, plan-gated); **no MIME/size validation** on any upload — listing images *or* the new confirmation photos (server signs whatever extension the client sends); no avatar/chat-image signed-upload path. `swap-confirmations` bucket now exists (Session 6). — added 2026-06-18, updated 2026-06-19
- [GAP] `chat-images` bucket READ is uploader-only → the other chat participant cannot view sent images (`0003_storage.sql` TODO) — added 2026-06-18
- [GAP] No health-check endpoint on `apps/api` — added 2026-06-18
- [DIVERGENCE] Design tokens: app uses a navy/green palette + Cairo font, **not** the spec's terracotta/sand/ink CSS-variable token system — added 2026-06-18
- [DIVERGENCE] Categories: 26 top-level categories with ~4 subcategories total, vs the spec's 8 categories × 4–6 subcats — added 2026-06-18
- [GAP] `seed.sql` hard-depends on migration `0004` running before it — added 2026-06-18
- [RESOLVED] ~~DB schema not applied to live Supabase~~ — applied Session 5 (2026-06-19): `reset.sql` + `full_setup.sql` run atomically via the session pooler (port 5432) using the existing `DATABASE_URL`, verified by row counts + schema sanity checks. `reset.sql` (new) restores the role grants `full_setup.sql` omits. **To re-apply after future schema changes:** run those two files in order (SQL Editor, or programmatically via the session pooler). — added 2026-06-18, resolved 2026-06-19
- [GAP] `full_setup.sql` itself sets **no role grants** — only safe to run as-is on a schema whose Supabase default grants are intact. After any `drop schema public`, the grants must be restored (now handled by `reset.sql`). Consider folding the grant block into `full_setup.sql` directly. — added 2026-06-19
- [CONFIRMED] Additive standalone migrations (0006, **0007**, **0008**, **0009**) applied to the live DB **without** re-running grants work correctly: `reset.sql`'s `alter default privileges … grant … to anon, authenticated, service_role` means every new table/function created afterward (by the pooler's `postgres` role) is auto-granted. Verified for `ratings` (S7), `notifications` (S8), and `blocks`/`blocked_between` (S11 — `blocked_between` is also given an explicit `grant execute` in 0009 since it runs inside the listings RLS on every feed read; verified an `authenticated`-role feed SELECT executes it without a permission error). So future additive migrations need no manual grant step as long as the schema wasn't dropped since. — added 2026-06-19
- [GAP] **Block listing-hiding is enforced in RLS** (`blocked_between` in the `listings`/`listing_images` read policies), which covers the **web** (it reads listings directly via the RLS client) on every surface — feed, search, featured, profile, saved, detail-by-id, following tab. The **service-role REST `/listings` browse path** (`ListingsService.list`, RLS-bypassed, no viewer id in its signature) does **NOT** apply block-filtering. The web feed does not use it; it's a latent gap for any future mobile/REST browse consumer (Phase 7). Block **messaging**-prevention IS covered on every path (backend guards + the hardened `messages` INSERT RLS + the RPC). — added 2026-06-19
- [PERF] The `auto_hide_reported_listing` trigger runs `count(distinct reporter_id)` over `reports` filtered by `(target_type='listing', target_id, status='pending')`; only `reports_status_idx` exists, so it's an index-on-status + filter (fine at current volume). If report volume grows, add a partial index `reports(target_id) where target_type='listing' and status='pending'`. — added 2026-06-19
- [HARNESS LEARNING] When verifying RLS via a raw `pg` connection (the migration-apply scripts), the pooler's `postgres` role has **`BYPASSRLS=true`**. `SET ROLE authenticated` alone DOES enforce RLS (an INSERT with no permissive policy is correctly rejected — this is the valid check). But **also setting `request.jwt.claims` from that BYPASSRLS session causes RLS to be skipped** (an env artifact, reproduced against an existing known-correct table). So: to test a *write-block*, use `SET ROLE authenticated` WITHOUT jwt claims; do NOT trust read-own/`auth.uid()`-filtered checks from this harness (they false-pass). Production is unaffected — the Supabase client reaches Postgres as `authenticated` via the no-bypass `authenticator` role. `notifications` RLS write-block was verified via the valid (no-jwt) path. — added 2026-06-19
- [EDGE] Proposal context card residual edge cases (reviewed, judged acceptable for MVP): (a) if the other party transitions the proposal while you have the **counter sheet open**, the Realtime refetch is deferred, so a stale counter submit returns a graceful backend 400 (no crash); (b) if the **target listing** is hidden/removed mid-negotiation it is RLS-filtered to null for the proposer's view → the "You get" column renders empty (guarded, no crash). Revisit when building Deal Closing. — added 2026-06-19
- [RESOLVED] `full_setup.sql` re-run failed on `create policy ... already exists` (storage-schema policies survive a public-schema reset). All `create policy`/`create trigger` statements (migrations `0001`/`0002`/`0003`/`0005` + `full_setup`) are now `drop ... if exists`-guarded → genuinely idempotent — fixed 2026-06-18
- [HARDENING] Proposal `create`/`counter` multi-step writes aren't a single transaction (service-role client). Mitigated by create-failure compensation + accept-time re-validation; full atomicity wants a Postgres RPC (`create_swap_proposal`) — added 2026-06-18
- [RESOLVED] ~~`swap_proposals` statuses `awaiting_confirmation`/`completed`/`disputed` have no transitions~~ — built in Session 6 Deal Closing. `awaiting_confirmation` is now cancellable (both parties keep a Withdraw path, so a stalled close can't permanently block the one-active-proposal index). All transitions are status-guarded conditional UPDATEs, race-safe against the completion RPC. — added 2026-06-18, resolved 2026-06-19
- [EDGE] Deal-closing residual edges (reviewed Session 6, acceptable for MVP): **(a)** a Withdraw/Dispute that loses the race to the other party's final confirm no-ops and returns the real (`completed`) state — correct, no corruption, but the actor briefly sees their click "do nothing" then the card flips to completed; **(b)** a dispute filed in that same race window inserts an admin report just before the swap completes → a stray "Swap dispute" report on a completed swap (rare; admin dismisses); **(c)** `getConfirmations` renders partial if a signed-URL generation fails for one photo (graceful — throwing would blank the whole card; Realtime/refresh recovers); **(d)** a confirmation photo re-uploaded with a different file extension orphans the previous object in the private bucket (harmless). — added 2026-06-19
- [TODO] No inventory lock: one listing can sit in multiple active proposals. `accept()` re-validates `active` status but does not reserve — decide a locking/auto-cancel strategy at Deal-Closing — added 2026-06-18
- [GAP] **Ban/suspend web read-level lockout is partial (Phase 6).** Session 12 added a `requireUser` gate that redirects banned/actively-suspended users away from every protected/account page, and the backend AuthGuard already blocks ALL their mutations. But **public read surfaces still render via RLS** for a banned user with a live session (no RLS-level `is_banned` check, no middleware sign-out). Full read lockout (RLS policy + middleware) is deferred to Phase 6. — added 2026-06-19
- [GAP] **No 2-hour admin session timeout (spec §4.1).** `/admin/login` reuses the standard Supabase session (default TTL + silent refresh); `requireAdmin` re-validates `is_admin` every request but never expires the session at 2h. Proper fix is a Supabase Auth session-lifetime config (or a stored admin-login-timestamp cookie checked in `requireAdmin`). Deferred to Phase 6. — added 2026-06-19
- [EDGE] Admin↔user conversation creation (`AdminService.getOrCreateConversation`) is a non-atomic check-then-insert with no unique constraint on the participant pair, so two concurrent admin "send message"/"request edits" on the same user could create a duplicate 1:1 thread (admin-only, low volume, cosmetic). Participant-insert errors now throw (no orphan thread). — added 2026-06-19
- [PERF] Admin analytics (`lib/admin-analytics.ts`) aggregates per-day/per-week buckets in JS over date-filtered selects (fine at MVP volume). If data grows, move the bucketing into SQL (`date_trunc`) RPCs. The reports queue (`fetchAdminReports`) also sorts/paginates a 500-row working set in JS (severity isn't a column) — it logs + paginates over the actual set so there are no blank pages, but very old reports beyond the cap aren't severity-ranked. The `/admin` analytics page pulls the **Recharts vendor chunk (~120 kB)** — acceptable (third-party), revisit in the Phase-6 bundle pass. — added 2026-06-19
- [NOTE] Admin **notes** about a user are stored as `admin_actions` rows (`action_type='note'`, admin-only RLS) — NOT a `profiles` column — so moderator notes never leak via the public-read `profiles` policy. `suspension_reason` IS on `profiles` (public-read, same posture as `email`/`phone`) because it is surfaced to the user. — added 2026-06-19
- [DECISION] **Web CSP is a pragmatic baseline (Session 13).** `next.config.mjs` ships a prod-only CSP with `script-src 'self' 'unsafe-inline'` + `style-src 'self' 'unsafe-inline'` (required by Next's inline hydration bootstrap + Tailwind/inline styles, no nonce). `connect-src` is derived from `NEXT_PUBLIC_SUPABASE_URL` (+ its `wss://` Realtime variant) + `NEXT_PUBLIC_API_URL`; `img-src 'self' data: blob: https:`. CSP is gated on `NODE_ENV==='production'` so `next dev` HMR (needs `unsafe-eval` + ws://localhost) isn't broken. **Tightening `script-src` with a per-request nonce middleware is a Phase-6 follow-up.** The always-on headers (HSTS/X-Frame-Options=DENY/nosniff/Referrer-Policy/Permissions-Policy) apply in dev too. — added 2026-06-20
- [GAP] **Rate limiting is in-memory + single-instance (Session 13).** `ThrottlerModule.forRoot` uses the default in-memory store, so counters are per-process — running multiple API instances multiplies the effective limit. A shared store (Redis via `@nest-lab/throttler-storage-redis` or similar) is needed for horizontal scaling. Also: `trust proxy` is **off by default** and opt-in via `TRUST_PROXY` — behind a reverse proxy you MUST set it (else all clients share the proxy IP and get collectively throttled), but only when the proxy strips inbound `X-Forwarded-For` (otherwise clients can spoof IPs past the limit). — added 2026-06-20
- [NOTE] **CORS tightened from `cors:true` to an allowlist (Session 13).** Browser callers must be listed in `CORS_ORIGINS` (defaults to `NEXT_PUBLIC_APP_URL` / localhost). Server-to-server SSR calls and native mobile requests send no `Origin` header, so they're unaffected. Empty/whitespace `CORS_ORIGINS` correctly falls back (uses `||`, not `??`) — an empty allowlist would otherwise block everything. — added 2026-06-20
- [EDGE] **`generateMetadata` double-fetch (Session 13).** `users/[username]` (and the pre-existing `listings/[id]`) call their fetch fn in both `generateMetadata` and the page body → 2 DB reads per request. Consistent with the existing listing-detail pattern; cheap to fix by wrapping the fetchers in React `cache()` if it ever matters. `sitemap.ts` is `force-dynamic` + try/catch-guarded so it never breaks `next build` without DB env. — added 2026-06-20

---

## Agent Instructions for Updating This File

**At the start of every session:**
1. Read "RESUME HERE" — this tells you exactly where to start
2. Skim the current phase checklist to orient yourself
3. Check Open Decisions for anything you need resolved before proceeding

**At the end of every session:**
1. Update the `[~]` / `[x]` / `[ ]` / `[!]` status on every item you touched
2. Update the "RESUME HERE" block:
   - Set **Current phase** to wherever you are
   - Set **Last worked on** to the specific feature/file you were working on
   - Set **Next task** to the single most important thing to do next
3. Add a row to the Session Log table (one-line summary + date if known)
4. Add any new open decisions or blockers you discovered
5. Resolve any Open Decisions you made a call on (note the decision and why)

**Never leave a session without updating this file.** It is the only way the next session can start without wasted time.
