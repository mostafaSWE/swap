# Swap — Mobile Release Plan & Session Tracker

> **Agent: read this file before touching `apps/mobile`. Update it before ending any session.**
> This plan **extends Phase 7 of `BUILD_PLAN.md`** ("Mobile Applications"). It does **not** re-document the web app, the backend, the DB schema, or Phases 1–6 — see `BUILD_PLAN.md`. This file tracks the mobile-specific work of getting `apps/mobile` from skeleton to **live on the Apple App Store and Google Play**.
> Format mirrors `BUILD_PLAN.md`: `[x]` done · `[~]` partial · `[ ]` not started · `[!]` blocked-on-human. Each phase has an **exit criterion**. Store-requirement facts are current as of **2026-07** and carry source links; **re-verify the ⚠ ones before submission — they change often.**

> **Sequencing (locked 2026-07-22):** build the **complete apps locally first**; developer accounts, store registration, listings, and compliance forms come **after** the app is done, not in parallel. So the build phases **M0–M6** come first, then a single hard stop, then everything that needs you (accounts/legal/submission) is consolidated in **Phase S** at the end. Native, not a PWA/Capacitor wrapper (a wrapper fails Apple Guideline 4.2).

---

## ▶ RESUME HERE

**Current phase:** **Phase M7 — Frontend Parity, UX Polish & Release-Candidate Visual QA (ACTIVE — Session 30, 2026-08-07)** — see the dedicated **▶ Phase M7** section below for scope, acceptance criteria, the screen-parity matrix, and batch-1/2/3 (homepage + nav → profile ecosystem + social graph → discovery: home/browse/detail/saved + shared card system) results. M0–M6 are a technically complete foundation but the frontend is not yet release-quality; M7 must pass before Phase S. *(Historical context on the earlier phases follows.)* **Phase M4 — Trust & Safety + Apple 1.2 (Session 25, 2026-08-05).** M0–M3 DONE & verified. **M4 is ~90% done: 4 of the 5 Apple-1.2 pillars are built + verified** — in-app **Report** (listing/user/message/conversation), **Block/unblock** (+ blocked-users screen), **published contact** (Support screen), and the **accept-EULA-before-posting** gate (versioned `TERMS_VERSION`, migration 0018, `TermsGuard` + RLS, mobile `TermsProvider` gating all 9 UGC writes). Also built: message-report server-side authz + admin-queue context, the 24h moderation **SLA runbook**, and a provider-agnostic **moderation seam**. **Verification:** typecheck 8/8, migration 0018 **applied to the live DB + verified**, **9/9 automated security tests** (5 RLS + 4 API), and a consolidated live pass (app renders; terms gate → accept → server-stamp → write proceeds, server-verified; Support + nav render). **The ONE remaining pillar — proactive content filter — is Decision D-2 (owner call):** the full brief with live 2026 pricing + a recommendation is in `docs/d2-content-moderation.md`; the seam is wired and inert until a provider is chosen. **Session 26 (2026-08-05) — M4 rollout fixes + M5 START.** Audited the live 0018 rollout: the deployed web is **NOT broken** (web writes go through the service-role NestJS API, which bypasses RLS; direct-Supabase writes are correctly blocked) — but the deployed API is still OLD (no `TermsGuard`), so terms aren't yet enforced on prod. Built the **web Terms-acceptance gate** (`getApi()` intercepts 403 `terms_not_accepted` → EULA modal → `POST /me/terms` → retry) so redeploying the new API won't 403 web users. Corrected the support contact to **`support@justswap.me`** everywhere. Hardened Terms enforcement on the public **bio** (migration **0019** trigger for the direct path + `ProfileService.updateMe` check for the API path) — **13/13** security tests. `/health` now honestly reports `moderation:{active:false}`. **Phase M5 (Android-first native integrations) — CODE + BACKEND COMPLETE, on-device pass pending an EAS rebuild.** Built A–E: native camera/library chooser, native Share, deep links + App Links + web well-known files, opt-in biometric app-lock, and the push foundation (migration 0020 + `PushModule` + mobile registration; MockProvider until FCM/APNs). Verified: typecheck 8/8, expo-doctor clean, m5-push 7/7, migrations 0019+0020 applied to the live DB. The new native modules (notifications/local-auth/secure-store) require one **EAS dev-client rebuild** before the consolidated Android pass. **Deploy — DONE this session:** web + API were **redeployed on Railway and smoke-verified on prod (12/12 behavioral checks)** — the M4 `TermsGuard`, web terms gate, `support@justswap.me`, push API, and well-known files are LIVE and enforcing. **Still pending an owner action:** the **EAS dev-client rebuild** for the on-device native pass (new notifications/local-auth/secure-store modules). **Owner also gates:** D-2 provider → wire `ContentModerationService`; FCM/APNs push creds; App-Link SHA-256/Team-ID; Google Play Child-Safety declaration + CSAE URL (Phase S).

**Session 27 (2026-08-06) — M5 Android VERIFIED ON-DEVICE + M6 config. HEAD `39409f1`.** Ran the EAS Android **development** build (`157a241a-13b8-4aa5-a282-1b2273f5945d`, SDK 57, commit `4035e31`) — finished, installed on `emulator-5554`, dev client loads the current JS bundle (Home renders; **no Fabric blank-render regression** with the new native modules). Consolidated on-device pass (signed in as @ahmed):
- **Deep links** — `justswap://listings/<id>` & `justswap://users/<name>` route correctly; `https://justswap.me/ar/listings/<id>` strips the locale; **authorized** conversation opens the chat, **unauthorized** conversation (@ahmed not a participant) shows an EMPTY chat — no messages/identity leak (RLS is the gate). **DEFECT FOUND + FIXED on-device:** `+native-intent` parsed `justswap://listings/<id>` with `new URL()`, which treated `listings` as the host and dropped it → Unmatched Route; now parses custom-scheme links by hand (query preserved for email `auth/confirm` links). (`39409f1`)
- **Image flows (M5-A)** — the native chooser (Take Photo / Choose from Library / Cancel) appears; **camera permission** requested on TAKE PHOTO; **denial** shows a graceful localized "Camera access needed" alert (Cancel only on first denial; Settings route reserved for permanent denial). **No mic permission** (audit). Two more static-review fixes: `acquireImages` now resolves `[]` on Android Alert dismiss (was a hung promise); `BiometricLock` locks synchronously on backgrounding (was a content-flash on resume). (`d6ca90c`)
- **Push (M5-E)** — notification-permission prompt fires at the right time (on `SIGNED_IN`); **without FCM creds `getExpoPushTokenAsync` soft-fails** (caught, non-fatal) so **no `device_tokens` row is created** and the app is unaffected. The `/me/devices` register/list/revoke API path itself is proven against prod (Session-26 smoke). **Real delivery needs FCM** — owner checklist in `docs/fcm-setup-checklist.md`.
- **Biometric (M5-D)** — the app-lock toggle correctly **hides when no biometric is enrolled** (`isBiometricAvailable` gating, confirmed on-device). Full lock/unlock cycle deferred to a **physical device** (emulator fingerprint enrollment is a `FLAG_SECURE` screen that can't be screenshot-driven); code reviewed + flash-fix applied.
- **Android permissions audit** — runtime-sensitive perms are only `CAMERA` / `POST_NOTIFICATIONS` / `USE_BIOMETRIC`+`USE_FINGERPRINT`; **no** location/contacts/microphone. **Correction after inspecting the production AAB's merged manifest** (the earlier read was the Android-14 dev-build `dumpsys`, which hides `maxSdkVersion`-capped perms): the manifest *declares* `READ/WRITE_EXTERNAL_STORAGE` **capped at `maxSdkVersion=32`** (from expo-image-picker — legacy picker support for Android ≤12; **do not apply on Android 13+**, which uses the scoped photo picker), and it **does** include **`SYSTEM_ALERT_WINDOW`** in production (NOT dev-only as first stated — `expo-dev-client`/`-launcher`/`-menu` declare no perms; it comes from a base RN library). `CHANGE_WIFI_MULTICAST_STATE` is confirmed dev-only (absent from the AAB). **Phase-S hygiene:** the app uses no draw-over-other-apps feature, so consider `android.blockedPermissions:["android.permission.SYSTEM_ALERT_WINDOW"]` before Play submission (verify it doesn't affect the debug redbox — prefer a release-variant block). ~15 OEM badge perms are benign normal perms from expo-notifications (unused; `shouldSetBadge:false`).
- **App Links** — extracted the real signing **SHA-256** (`7A:F9:…:E7`, EAS default keystore `UNNI7HMw3L`) via apksigner → wrote it into `assetlinks.json` (`af6a2f4`) → **redeployed web** → live (HTTP 200, `application/json`, no redirect); `pm get-app-links` shows the app declares `justswap.me` with a matching signature (verification force-set for the on-device routing test; real auto-verify happens on a Play-Services device). The **Google Play App Signing** key fingerprint must be appended at Phase S.

**M6 config (`d6ca90c`):** iOS `privacyManifests` (required-reason APIs: UserDefaults CA92.1, FileTimestamp 0A2A.1/C617.1, SystemBootTime 35F9.1, DiskSpace E174.1); a flattened **no-alpha, full-bleed `icon-ios.png`** wired via `ios.icon` (App Store rejects alpha; source icon has alpha + pre-rounded corners — final full-bleed re-export is an Apple-gated submission nicety); `android.versionCode`/`ios.buildNumber`; `docs/fcm-setup-checklist.md`. **Android production AAB** (`production` profile, app-bundle): the **first attempt (`91a29340`) FAILED** at `:app:createBundleReleaseJsAndAssets` with `Cannot find module 'babel-preset-expo'` — `babel.config.js` uses the preset but it was only a transitive dep of `expo`, unresolvable from `@swap/mobile` under pnpm's strict isolated linker (the dev build never bundles release JS, so it passed). **Fixed** by adding `babel-preset-expo ~57.0.5` as a direct devDep (`5f074e9`); **rebuild `0aa6e4f3` succeeded + validated**: well-formed AAB (`BundleConfig.pb`, `base/dex/classes{1..5}.dex`, `base/manifest`, `base/resources.pb`), package `me.justswap.app`, versionCode 1, **signed SHA-256 = the same `7A:F9:…:E7` upload keystore** (matches assetlinks; Google Play App Signing will re-sign for distribution — Phase-S fingerprint). iOS signing / APNs / Team-ID / TestFlight remain **blocked on the Apple Developer account**. **§7 validation:** typecheck 8/8, expo-doctor (only the benign react 18/19 web dup), `git diff --check` clean, secret scan clean, translation parity web 647/647 + mobile 668/668, m4-trust-safety 13/13, m5-push 7/7. **No prod test data created** (no listings/messages/reports; @ahmed has 0 device_tokens; emulator test-PIN cleared).

## ▶ Phase M7 — Frontend Parity, UX Polish & Release-Candidate Visual QA (ACTIVE, Sessions 28–30, 2026-08-06 → 08-07)

**Why this phase exists:** the app is technically complete but **not release-quality visually** — it shipped emoji tab icons, a generic header, and a category-chip wall, none of which match the responsive website. This phase brings every user-facing screen to production quality using the **website's phone viewport as the blueprint**, adapted to native conventions. **It must pass before Phase S.** Do not describe the app as release-ready until M7 passes.

**M7 acceptance criteria (all required):** no emoji interface icons; homepage no longer dumps all categories; homepage has a polished hero + primary actions + search/location + curated categories + structured content comparable to the web; nav uses professional icons + polished active states; every primary route audited vs its web counterpart; loading/empty/error/success/disabled states visually complete; AR-RTL + EN-LTR both pass; small + large phone sizes pass; accessibility + touch-target checks pass; motion smooth + respects reduce-motion; a production-like Android build shows the redesign; a **physical device** passes final visual QA; screenshots are store-suitable. Technical tests (typecheck/security/AAB) prove correctness, **not** design quality.

**Screen-parity matrix** (web route → mobile route → status). ✅ done · 🟡 partial/in-progress · 🔴 not started (still the M1 shell):

| Web | Mobile route | Status | Notes / required correction |
|---|---|---|---|
| `/` Hero+sections | `(tabs)/index` | ✅ **M7 batch 3** | Batch 1 hero/categories/how-it-works/featured **+ batch 3**: Latest Listings rail (deduped vs Featured), Trust & safety (3 cards), final CTA card, pull-to-refresh, per-rail skeletons, error+retry, empty-with-Add. Full website section order. Verified EN+AR. |
| global header/nav | `(tabs)/_layout` + hero header | ✅ **M7 batch 2** | Reassessed vs web: Home/Browse/**center green Add FAB**/Messages/Profile (stock `<Tabs>` bar, lucide via `Icon`); **notifications lifted out of the bar into a header bell** (`HeaderBell` via stock `headerRight`); branded navy headers on Browse/Messages/Profile. Add routes to new-listing via a `tabPress` listener. Verified EN+AR. |
| followers/following | `connections/[username]` | ✅ **M7 batch 2** | Tappable profile counts → segmented Followers/Following list; per-row Follow/Unfollow; block-safe + paginated via `list_follows`. Verified EN+AR. |
| `/listings` browse+filters | `(tabs)/browse` | ✅ **M7 batch 3** | Branded header + bell; search (debounced), TOP_LEVEL category chips, sort, Filters (country/city/condition/featured) + Clear; **results count**, enriched cards, pull-to-refresh, end-of-results; distinct **error+retry** + lucide empty (no more `🔍`) + filter-empty w/ Reset; seeds filters from route params. Verified EN+AR. |
| `/listings/:id` | `listings/[id]` | 🟡→✅ **M7 batch 3** | Gallery **page indicator** (N/M pill + dots, RTL-safe) + Featured overlay; **Description** heading; signed-out Save → `/login`; real **not-found** state (icon + Browse); Heart → Bookmark; sticky bar safe-area inset. Remaining: message-vs-propose primary decision; optional fullscreen viewer; country/posted-date meta. |
| `/users/:name` | `users/[username]` | ✅ **M7 batch 2** | Public-profile parity: shared `ProfileHeader` card (avatar/name/@user/member-since/bio/rating/swaps + tappable follower/following counts), Follow, block-safe Message, Share (header), Block/Report, self-view actions hidden, Listings section. Verified EN+AR. |
| `/profile` (own) | `(tabs)/profile` | ✅ **M7 batch 2** | Redesigned: polished card (avatar+fallback, name, @user, member-since, bio, rating, real counts), primary **Add New Listing**, Edit/Saved/Settings tiles, listings section (count/grid/empty-with-CTA/loading), pull-to-refresh, separated Sign-out. No emoji; admin tile only for admins. Verified EN+AR. |
| `/saved` | `saved` | ✅ **M7 batch 3** | FlatList + card skeletons; lucide Bookmark empty w/ Browse CTA; signed-out Sign-in CTA; pull-to-refresh; **refetch-on-focus** (unsaved-from-detail drops on return); error+retry; bottom safe-area inset. Verified EN+AR. |
| `/login`,`/register`,`/reset`,`/onboarding` | `login`/`register`/`reset-password`/`forgot-password`/`onboarding` | 🟡 | Functional forms (Session-25/24); polish spacing/hierarchy/states vs web. |
| `/new-listing`, edit | `new-listing`, `listings/[id]/edit` | 🟡 | 3-step form works; image chooser M5-verified; polish step UI/validation states. |
| `/messages`, chat | `(tabs)/messages`, `messages/[id]` | 🔴/🟡 | Emoji `💬`; inbox list + chat bubbles need web-parity polish. |
| proposal/deal/rating | in `messages/[id]` (`ProposalContextCard`) | 🟡 | Works (Session-21); polish proposal-state visuals + completion. |
| `/notifications` | `notifications` (root route) | ✅ **M7 batch 2** | Moved OUT of the tab bar to a root-stack route reached from the header **bell** (live unread via `useUnreadCount`); lucide Bell empty state. |
| `/settings` hub | `settings` | ✅ **M7 batch 2** | New grouped-tile settings hub (Blocked users · Contact support · Terms) with green IconTiles; app-lock toggle is hardware-gated. |
| `/settings/blocked`,`/support`,`/terms`, reports, biometric | `blocked`,`support`,`terms`, report sheets, `BiometricLock` | 🟡 | Functional (M4/M5); reached from the settings hub; visual polish + states pass. |
| cross-cutting states | all | 🟡 **(batch 3 primitives)** | New shared **`ErrorState`** (message+Retry) + **`ListingCardSkeleton`** replace the silent `.catch(()=>set([]))` fake-empty + bare spinners on home/browse/saved; EmptyState uses lucide nodes. Still to sweep: messages/proposal/auth/create + offline. |

**M7 batch 1 (Session 28) — Global shell + navigation + homepage:** replaced the emoji tab bar with **lucide-react-native** icons (Home/Search/MessageCircle/Bell/UserRound) via the shared `Icon` primitive, kept the Alerts unread badge, and hid the home header. Rebuilt `(tabs)/index.tsx` + a new `src/components/home/HomeHero.tsx`: branded hero (View-composed accent glow + faint `Repeat2` motif, RTL-aware; `Animated` fade+rise entrance gated on `AccessibilityInfo.isReduceMotionEnabled`), `JustSwap` wordmark header + account action, badge/headline/subtitle from the existing `home.*` catalog, **Add listing** + **Browse categories** CTAs, a tappable search + location bar, a **How it works** horizontal step-card rail, a **curated top-6 category grid** (lucide `CategoryIcon`, "View all") replacing the chip wall, and a **Featured** listing rail. Replaced `ListingCard`'s `🖼️` placeholder with a lucide `ImageOff`. **Verified on `emulator-5554`** (EAS dev client + cleared Metro): **EN-LTR** and **AR-RTL** both render correctly (AR mirrors the wordmark/arrow/glow/tab order and matches the web Arabic reference); renders without clipping at a 360dp small viewport; mobile typecheck 8/8.

**M7 batch 3 (Session 30, 2026-08-07) — Discovery parity: home completion, browse, listing detail, saved + a shared card system (web + mobile):**
- **Shared card system (critical path):** `ListingCard` rebuilt on `ItemArtwork` (category-tinted placeholder when imageless) with a Featured (Star) + condition overlay, a two-line title, category · city, and a compact "Wanted in Exchange" line; the whole card is one accessible button (`Card` now forwards `accessibilityLabel`/role). New **`ErrorState`** (message + Retry) and **`ListingCardSkeleton`** primitives replace the silent `.catch(()=>setItems([]))` fake-empty and bare spinners.
- **Home:** added the missing website sections — **Latest Listings** (deduped vs Featured), **Trust & safety** (3 cards), a final **CTA** card — plus pull-to-refresh, per-rail skeletons, an error+retry surface, and empty boxes with an Add CTA. The section order now matches the web (hero → search → how-it-works → categories → featured → latest → trust → CTA).
- **Browse:** killed the `🔍` emoji empty (lucide `PackageSearch` + hint + Reset), added a **results count**, an **error+retry** state, **pull-to-refresh**, an **end-of-results** footer, **`TOP_LEVEL_CATEGORIES`** chips, and seeds all filters from route params (search/country/city/condition/sort).
- **Saved:** FlatList + card skeletons; lucide Bookmark empty w/ Browse CTA; signed-out Sign-in CTA; pull-to-refresh; **refetch-on-focus** (items unsaved from detail drop on return); error+retry; bottom safe-area inset.
- **Listing detail:** gallery **page indicator** (N/M pill + dots, RTL-safe index) + Featured overlay; signed-out **Save → `/login`** (was `/profile`); a real **not-found** state (icon + Browse); a **Description** heading; Heart → **Bookmark**; sticky-bar safe-area inset.
- **Web defects fixed** (found in the audit): `ReportDialog` no longer shows on the owner's own listing (`!isOwner`); signed-out report is no longer a dead-end (a sign-in prompt + explicit error message); the Saved empty state uses a saved-specific string (was the search-flavored `listings.empty`).
- **Device-verified on `emulator-5554` (EN-LTR + AR-RTL):** home (all new sections), browse (results count + enriched cards + states), listing detail (Description heading + safe-area), with correctly-mirrored card overlays in AR (Featured badge leading, condition pill trailing) — 0 JS errors. Validation: ws typecheck **8/8**, web `next build` (68/68), i18n parity web 654/654 + mobile 672/672, **m7-follows 18/18**. **Deferred (documented):** owner-can't-see-non-active-listings (needs an additive `getListings` status param — query work, both surfaces), web browse pagination, `SearchBar` location desync, listing-card owner-row/view-count, `ListingPicker` unification — see "M7 remaining".

**M7 batch 2 (Session 29, 2026-08-07) — Profile ecosystem + social graph + shell reassessment (web + mobile):**
- **Backend/DB (shared, already live):** migration **0021** — `list_follows(target, direction, limit, offset)` RPC returning the public-profile columns **+ per-row `is_following`**, block-safe (`auth.uid() is null or not blocked_between(...)`, so anonymous/public browsing still works), excludes admins/banned, stable `created_at desc, id` order, capped `limit ≤ 100` with offset; index `follows_following_idx`. Also fixed `sync_follow_counts()` → **SECURITY DEFINER** (RLS-client follows now update the followed user's denormalized `followers_count`) + a one-time reconcile. Mirrored into `full_setup.sql`. Shared `getFollowers`/`getFollowing`/`FollowDirection` in `@swap/api`; `PublicProfileWithFollow` type.
- **Mobile:** own-profile **redesign** (polished `ProfileHeader` card with tappable counts, primary Add, Edit/Saved/Settings tiles, listings section with states, pull-to-refresh, separated sign-out); **public-profile parity** (shared card + Follow/Message/Share/Block/Report, self-view actions hidden); new **`connections/[username]`** followers/following screen (segmented, per-row `FollowButton`, optimistic toggle, pagination, states); new **`settings`** grouped-tile hub; **notifications moved to a root route reached from a header bell**; **nav reassessed** onto the stock `<Tabs>` bar with a prominent green **center Add FAB** + header bell. **Device-verified on `emulator-5554` (EN-LTR + AR-RTL):** nav, own profile (real data — 4 followers/1 following/8 listings/★4.0), followers list, public profile, Follow→Following→unfollow (optimistic + persisted + reverted), settings; AR mirrors the shell/stat-row/tiles/segmented/rows and back-arrow correctly.
- **Web:** followers/following **counts made clickable** in the shared `ProfileHeader` (both own + public); new **`/users/[username]/connections?tab=`** route (server-rendered first page + `ConnectionsList` client "load more" via the same block-safe RPC); `fetchFollowers`/`fetchFollowing` server wrappers. Build ✅; runtime-verified EN + AR/RTL render the real 4-follower / 1-following lists; RTL via `dir="rtl"`.
- **Verification:** workspace typecheck **8/8**; web `next build` ✅; translation parity web 650/650 + mobile 671/671; **m7-follows 18/18** (block hides from lists, unblock un-hides visibility, no private columns, counts match, pagination no dups); **m4-trust-safety** RLS A/B/C pass (D/E needs the local NestJS server running — unrelated to this batch); `git diff --check` clean; secret scan clean.

**M7 batch-2 debugging note (do not repeat):** the profile/nav rebuild first white-screened, then hard-**native-crashed**. Two distinct bugs: **(1)** a route **collision** — expo-router cannot have both a leaf file `users/[username].tsx` **and** a folder `users/[username]/` (I'd put `connections.tsx` inside it); the ambiguous route tree throws at the **root** layout → white screen, no redbox. Fix: a flat sibling route `connections/[username].tsx`. **(2)** a **custom `tabBar` prop and/or per-screen custom `header` option** reproducibly **native-crashes** this Fabric/New-Arch build (Hermes render throw in `libhermesvm`→`jsi::Function::call`, app closes, no JS redbox) — the SAME class of failure as batch 1's `tabBarStyle.height`/direct-lucide-in-`tabBarIcon`. **Lesson: on this Fabric build, do NOT replace the stock tab-bar/header chrome — deliver nav via supported `<Tabs>` options only** (`tabBarIcon` with the `Icon` wrapper, a `tabBarIcon` FAB View for center Add, `headerRight` for the bell, `tabPress` listeners for actions). Also this session: Windows resource exhaustion (15 orphaned node processes, ~1 GB free) made Metro's `expo-updates runtimeversion:resolve` subprocess crash with `0xC0000142` (STATUS_DLL_INIT_FAILED) → "Failed to open app"; **kill all node + stop the emulator to reclaim RAM**. To reach the app root past the dev-client `?url=` deep link (which lands on "Unmatched Route"), load from the launcher's `localhost:8081` card, not the deep link. AR verification: `EXPO_PUBLIC_LOCALE=ar` in Metro's env forces the locale (inlined at bundle time); the boot guard flips `forceRTL` + reloads behind the splash.

**M7 batch-1 debugging note (do not repeat):** the redesign first white-screened. Root cause was in `(tabs)/_layout.tsx` — a **fixed `tabBarStyle.height` and/or rendering the lucide icon component directly in `tabBarIcon` on the New Architecture (Fabric)** crashed the tab navigator with no redbox (render throw before splash hides ⇒ white). Fix: render tab icons via the shared `Icon` wrapper and drop the fixed tab-bar height. Also: `react-native-svg` `<Svg>`+`RadialGradient`/`Rect` **fill-on-Fabric** doesn't apply (ViewManager setters "not found") — build hero backgrounds from plain Views, not SVG. And Metro's Windows watcher went stale after rapid edits (rebuilds showing "1 module"); a `--clear` restart (after killing the real node PID on 8081 — `TaskStop` leaves it alive) fixed it. Dev-loop RTL toggle: `adb shell cmd locale set-app-locales me.justswap.app --locales ar-SA` (Android 14 per-app locale) then reconnect; the boot guard reloads to RTL behind a brief white splash.

**M7 remaining (next batches):** **messages** (`💬` inbox + chat bubbles) + proposal/deal/rating visuals; auth/login/register/reset/onboarding polish; create/edit-listing step UI + validation states; safety/legal + notifications visual pass; cross-cutting offline/disabled/success sweep on the not-yet-touched screens; large-phone + **physical-device** visual QA; consolidated web-vs-native EN/AR screenshot comparison set. **Functional/parity gaps deferred (documented, need backend or larger work):** owners can't see non-active (paused/completed) listings anywhere — `getListings` hard-filters `status='active'`; needs an additive owner/include-status param + a paused/completed card state on **both** surfaces; web browse has **no pagination** (single `limit:48`); web `SearchBar` location label desyncs from the advanced filter; listing-card owner-row + view-count; unify `ListingPicker` onto the shared card. **Earlier deferrals still open:** truly *raised* center FAB (custom tab bar crashes Fabric); message-vs-propose primary-action decision on listing detail. *(Discovery + profile/social screens are now polished, but **M7 is NOT complete** — messages/auth/create/proposal + physical-device QA + the comparison set remain.)*

**Dev runtime (updated 2026-07-25):** M0–M3 were developed in a **local Android development build** (`expo run:android` + `expo-dev-client`) — never Expo Go. **EAS is now the verified native-build path:** the Android `development`-profile cloud build runs on the emulator as a dev client connected to local Metro. Do not resume the Windows native-toolchain investigation unless future native debugging genuinely requires it.

**What's DONE:** `apps/mobile` runs on **Expo SDK 57** (RN 0.86 / React 19.2.3, New Arch on); reaches the **live backend** via shared `@swap/api` (Supabase auth + **AsyncStorage persistence**, wired `SwapApiClient`, RLS reads, Realtime); the cold-start-logout root cause (`createSupabaseClient` ignoring a storage adapter) is **fixed**; bundle id **`me.justswap.app`**, name **"JustSwap"**. **Build infra:** Android env persisted (User scope); NDK 27.1.12297006 / CMake 3.22.1 / platform-36 / build-tools 36 installed; `expo-dev-client` + `expo prebuild` (android/ + ios/ gitignored — CNG). The Windows New-Arch path wall is handled by **machine-local setup only** — `LongPathsEnabled=1` + **ninja 1.12.1** (long-path-aware) + a native-build parallelism cap (low free RAM). **No repo resolution change:** `nodeLinker` stays pnpm's default **isolated** (phantom-dep-safe; see Known gaps). **Typecheck 8/8 green**; `expo-doctor` 19/20.

**✅ M0 verified — the build works and the app reaches the live backend.** The local Android dev build succeeds and the M0 smoke passed **5/5** on `emulator-5554`: Supabase auth (`signin=ok`), **session persistence across a cold restart** (`boot-session=ahmed@swap.demo`), RLS read via `@swap/api` (`count=6`), Realtime (`SUBSCRIBED`), authenticated REST `/me` (`user=@ahmed`). The Windows path-length wall took **two** fixes: `LongPathsEnabled=1` (you, elevated) **+** upgrading the CMake-bundled **ninja 1.10.2 → 1.12.1** (long-path-aware — LongPaths alone wasn't enough because the old ninja isn't manifested for it).

**Last worked on:** **M4 build + verification (Session 25, 2026-08-05).** Scouted the whole trust-&-safety surface (7-agent workflow) + researched D-2 providers (4-agent workflow). Built: mobile **report** (all 4 targets) wired to `api.reportListing`/`createReport`; **block/unblock** (profile + chat + `app/blocked.tsx`); **support** + **terms** screens; the versioned **terms-acceptance gate** (config `TERMS_VERSION`, migration 0018 = profiles cols + `has_accepted_current_terms()`/`current_terms_version()` + listings/messages/reports RLS + dup-report index, `TermsGuard` + `POST /me/terms` + `TermsProvider.ensureAccepted()` on 9 UGC writes); message-report **server-side participation authz** (`ReportsService` + RLS) + web admin-queue enrichment; the **moderation seam** (`ModerationModule`, no-op); the **24h SLA runbook** + the **D-2 brief**. **Verified:** typecheck 8/8; migration 0018 applied to the live DB + all objects checked; **9/9 security tests** (`apps/api/test/m4-trust-safety.mjs` — RLS + API, reversible); consolidated live pass (terms gate end-to-end, server-verified; Support/nav render). Demo terms-acceptance state reset (reversible). **D-2 provider choice = the one open owner decision.**

**Earlier (Session 17–18, 2026-07-23):** Session 18: built + verified the **forgot-password request** screen (`resetPasswordForEmail`, always-"sent") + a "Forgot password?" login link (`309e62c`). Session 17: built + verified the **swap proposal negotiation loop** — `ProposeSwapSheet` + `ListingPicker` → `createProposal`, `ProposalContextCard` (role-flipped give/get, status badge, turn banner, turn-gated Accept/Counter/Decline/Withdraw), live status via `subscribeToProposal`; verified cross-account (@ahmed→@sara): create → counter → status/turn transitions; inbox badges render. Deal-closing deferred to M5 (needs `expo-image-picker`). **Committed locally only** (`42653b2`, `309e62c`, not pushed). (Session 16: M3 messaging, `f49da97`. Session 15: M3 auth, `c380945`. Session 14: M2 read surface COMPLETE.)

> ✅ **Git state (Session 27, 2026-08-06):** Session 24–26 commits are **pushed** (Session-26 HEAD `1d9cf22`). **Session 27** adds `4035e31` (HEAD-fix), `d6ca90c` (M6 config + M5 fixes), `af6a2f4` (App-Links fingerprint), `39409f1` (deep-link fix) + the tracker/report commits — see the Session-27 block above (HEAD `39409f1` at time of writing). **Deploy status:** migrations 0018 + 0019 + 0020 are **applied to the live Supabase DB**, and the Railway **web + API services are now REDEPLOYED** with the M4/M5 code and **smoke-verified on production — 12/12 behavioral checks** (TermsGuard returns 403 `terms_not_accepted` → `POST /me/terms` stamps acceptance → the same write is no longer terms-blocked; the profile-bio gate; push register→list→revoke; `/health` `moderation:{active:false}`; well-known files served as `application/json`; `support@justswap.me` on the live Support page). The whole run used one throwaway user and cleaned up with no leftover data. `docs/monetization-plan.md` stays untracked (out of scope).

> **Historical Session-19 local-build diagnosis — superseded by the Session-20 EAS resolution below; retain for forensic context only.**
> 🛑 **DEV BLOCKER (open, 2026-07-23) — app renders blank after the create-listing prebuild.** The full `expo prebuild` + rebuild (to link `expo-image-picker`) succeeds and the module links, but the running app now **mounts no views** — React reconciles (added `[boot]` logs confirmed `ready=true`) yet even a raw `<View style={{background:red}}>` paints nothing and `uiautomator` dumps an empty hierarchy (so it's a real New Arch/**Fabric mounting** failure, not a swiftshader/screencap artifact). This is **whole-app**, not create-listing-specific. **Session 19 update — strong root-cause signal + `--fix` tried:** ran `npx expo install --fix` (reconciled ~9 packages: expo core, **react-native-screens 4.25.2→4.26.2**, expo-updates, expo-router, expo-dev-launcher, etc.) + deleted `build/generated` for fresh codegen + rebuilt. The rebuild **failed** on: `react-native-screens/.../RNSFormSheetHostShadowNode.h: fatal error: 'react/renderer/components/rnscreens/EventEmitters.h' file not found` — a **react-native-screens Fabric codegen** miss. Since `react-native-screens` powers the expo-router `Stack`, a stale/mismatched screens Fabric codegen ⇒ its native components never register ⇒ the whole nav subtree mounts nothing ⇒ **the blank render**. This is almost certainly the root cause. Fixing it needs a **`gradlew clean` full rebuild** (regenerate codegen + C++ together) — the from-scratch rebuild the user asked to avoid; per the time-box, **stopped here.** The RTL early-set (below) is ruled out and the MainApplication injection was verified correct (standard SDK-57 template + clean early-set). `expo install --fix` changes are **uncommitted** (revertable). **→ Decision pending:** local `gradlew clean` rebuild (OOM-prone, ~10 min) **vs. EAS Build** (the user's stated direction — cloud build sidesteps the whole local-native-build class after long-paths/ninja/clang-OOM/gradle-OOM/codegen fights across 3 sessions).

**Fix path to try next (in order):** (1) ~~RTL early-set~~ — **TESTED & RULED OUT** (Session 19): commented out the `I18nUtil.forceRTL(...)` block in `MainApplication.onCreate` + rebuilt → still blank (uiautomator hierarchy still empty). Not the cause. (2) **`npx expo install --fix`** to reconcile the ~9 drifted packages (screens/updates/router/expo core) + a **clean** rebuild — now the leading suspect (a New-Arch codegen/version inconsistency surfacing only after the from-scratch prebuild). Note `expo-image-picker@57.0.6` installed under pnpm "loose mode" (immature version) — consider pinning it to the SDK-matched version. (3) Diff the regenerated `MainActivity.kt`/`MainApplication.kt` + the New-Arch/`DefaultNewArchitectureEntryPoint`/reanimated setup vs a known-good bare RN 0.86 template; or a full uninstall + from-scratch clean build. (4) Worst case, verify on a device/emulator with a real GPU. **Decision made (Session 20): try the EAS cloud Android development build before any more local-native diagnosis.** The project is linked as `@most.othman/justswap`; build `66e95688-e3de-48e1-ae29-db3a917265fc` is in progress. **All feature CODE (create-listing incl.) is typecheck-clean and native-linked — only on-device UI verification is blocked.** Dev-loop reminders learned here: run Metro in **watch mode (no `CI=1`)**, re-run **`adb reverse tcp:8081 tcp:8081`** after any emulator reboot, and free RAM (kill emulator) before a native build. (Session 9: icons + Select/BottomSheet + forms. Session 8: inventory + expo-localization + primitives. Session 7: RTL-first i18n. Session 6: nodeLinker→isolated. Sessions 4–5: M0 + nav shell.)

> ✅ **Session 20 resolution (2026-07-25):** EAS build `66e95688-e3de-48e1-ae29-db3a917265fc` completed successfully, including `react-native-screens` across the Android ABIs. Its installed cloud APK rendered the full Fabric app normally; Android's system picker returned an image, `Cloud_Build_Verification` published, opened in detail, and persisted after a cold dev-client restart. The blank mount is therefore isolated to the Windows-local native build path; no feature or RTL change is required. The native RTL early-set config plugin was present in the EAS build and the English emulator remained LTR.

**Next task (in order):**
1. **Owner-gated unblocks** (no more independent Android work remains without these): **D-2** proactive-filter provider → wire `ContentModerationService`; **FCM** (Firebase project + FCM v1 key, `docs/fcm-setup-checklist.md`) → real Android push; **Apple Developer membership** → iOS signing/APNs/Team-ID/TestFlight; **Phase S** store accounts/legal/listings/submission.
2. **Biometric full lock/unlock cycle** on a physical Android device (emulator fingerprint enrollment is a `FLAG_SECURE` screen; toggle-gating is already device-verified).
3. Retain a literal **two-physical-device** full-lifecycle pass for Phase-S final release QA. iOS remains blocked by the paid Apple account.

> **Dev loop:** start the Android emulator, install the EAS development APK, then run fresh watch-mode Metro (`pnpm start -- --dev-client`, port 8081) and `adb reverse tcp:8081 tcp:8081`; the dev client reconnects to the local JS bundle. The local NestJS API is port 4000. The connectivity harness is at route `/m0-check` (set `EXPO_PUBLIC_M0_AUTOTEST=1` to auto-run it). EAS is now the preferred native-build path; do not use the local Windows build unless a future native debugging task specifically requires it.

> **Reality check (corrected Session 28):** M0–M6 produced a **technically complete, buildable foundation** (auth, live data, RLS, M4 safety, M5 native integrations, EAS dev+production AAB) — but that is **not the same as release-quality frontend/UX**, and the earlier "only owner/external items remain" framing was **wrong**. The native UI was a functional shell with **emoji tab icons, a generic header, and a category-chip wall** — far below the responsive website's polish. **Phase M7 (Frontend Parity, UX Polish, Release-Candidate Visual QA) is now the active engineering phase and MUST pass before Phase S.** So the honest status is: **M0–M4 done; M5 Android verified; M6 config + AAB done; M7 IN PROGRESS (homepage + navigation redesigned & device-verified EN/AR — the rest of the screens pending).** External/owner gates (D-2 provider, FCM creds, Apple Developer membership for all iOS signing/APNs/Team-ID/TestFlight, Phase-S store submission) still apply **in addition to** M7 — they are not the only remaining work.
>
> **Root-cause note for M1's insufficient acceptance criteria:** M1 accepted "a navigable shell" (emoji icons, minimal home) as done. That bar was too low — it never required **production-quality visual parity with the website**. Recorded here so it is not repeated: a screen is not "done" because its API calls work; it is done when it matches the website's brand, hierarchy, states, and polish (see M7 acceptance criteria).

---

## Session Log

| # | Summary | Ended on |
|---|---|---|
| 30 | **M7 batch 3 — discovery parity: home completion, browse, listing detail, saved + a shared card system.** First: **deployed the Session-29 web code to Railway + prod-smoke-tested** it on justswap.me (followers/following live EN+AR, 404 on bad usernames, no leaks) and corrected the trackers' premature "redeployed" claim (commit `1f35230`). Closed the **batch-2 gaps** (`2bea38d`): optimistic follower-count sync on the public profile (±1, clamped, roll-back) + Add-tab a11y/polish. Then a **6-agent parallel audit** → built the discovery milestone: a unified **`ListingCard`** (ItemArtwork + Featured/condition overlays + wanted-exchange line + a11y) and shared **`ErrorState`**/**`ListingCardSkeleton`** primitives; **home** gained Latest/Trust/CTA + pull-to-refresh + skeleton/error/empty states (full web section order); **browse** got a results count, error+retry, refresh, end-of-results, TOP_LEVEL chips, param seeding, and lost the `🔍` emoji; **saved** got refetch-on-focus + refresh + skeletons + real empty/signed-out/error states; **listing detail** got a gallery N/M+dots indicator, signed-out Save→login, a real not-found, a Description heading, Bookmark, and a safe-area sticky bar. Also fixed 3 **web** defects (Report owner-guard + signed-out sign-in + saved-empty copy). **Device-verified EN-LTR + AR-RTL** on `emulator-5554` (home all-sections, browse, detail; AR card overlays mirror correctly), 0 JS errors. ws typecheck 8/8, web build 68/68, i18n parity (web 654/654, mobile 672/672), m7-follows 18/18. **Web redeployed** (defect fixes) + prod 200s. Commits `1f35230`,`2bea38d`,`3e8f6b4`,`64c6069` (+ tracker). M7 still has messages/auth/create/proposal + physical-device QA. | 2026-08-07 |
| 29 | **M7 batch 2 — profile ecosystem, social graph & shell reassessment (web + mobile).** Shipped the full followers/following social graph on **both** surfaces off one block-safe `list_follows` RPC (migration 0021, already live; also fixed the `sync_follow_counts` trigger to SECURITY DEFINER so RLS-client follows update the denormalized count). **Mobile:** redesigned own profile (polished card, tappable counts, primary Add, Edit/Saved/Settings tiles, listings section + states, pull-to-refresh), public-profile parity (shared card + Follow/Message/Share/Block/Report, self-view hidden), a new `connections/[username]` followers/following screen (segmented, per-row Follow, pagination), a grouped `settings` hub, and **reassessed the nav** — notifications lifted from the tab bar into a **header bell**, a prominent green **center Add FAB**, Home/Browse/Add/Messages/Profile. **Web:** clickable follower/following counts + a `/users/[username]/connections` route (SSR + "load more"). **Device-verified EN-LTR + AR-RTL** (nav, own profile with real data, followers list, public profile, follow→unfollow optimistic+persisted+reverted, settings). **Root-caused two crashes:** an expo-router **file+folder route collision** (`[username].tsx` + `[username]/`) white-screened the root → flat `connections/[username]`; and a **custom `tabBar`/`header` native-crashes Fabric** (same class as batch 1) → rebuilt on the stock `<Tabs>` bar. Validation: ws typecheck 8/8, web build ✅, i18n parity (web 650/650, mobile 671/671), m7-follows 18/18, m4 RLS A/B/C pass, diff-check + secret scan clean. **Web DEPLOYED to Railway (2026-08-07, commit `71b28f1`) + prod-smoke-tested on justswap.me:** public-profile counts are clickable links, followers/following render real data (4/1) in **EN + AR** (`dir="rtl"`), invalid usernames return a proper **404**, no private-column leak, core pages (`/en`,`/ar`,`/listings`,`/users/ahmed`) all 200; API unchanged (`/health` ok — no API redeploy). M7 still has browse/messages/others + physical-device QA remaining. | 2026-08-07 |
| 28 | **Phase M7 OPENED (frontend parity/UX) + batch 1: global shell, navigation & homepage.** Corrected the prior "engineering complete / only owner-items remain" framing — the native UI was a shell (emoji tabs, generic header, category-chip wall) far below the website. Added Phase M7 (must pass before Phase S) with acceptance criteria + a screen-parity matrix, and recorded the M1-criteria gap. **Batch 1 (device-verified EN-LTR + AR-RTL on emulator-5554):** replaced emoji tab icons with lucide (Home/Search/MessageCircle/Bell/UserRound) + kept the Alerts badge; rebuilt the homepage — branded `HomeHero` (accent glow + swap motif, RTL-aware, reduce-motion-gated entrance), `JustSwap` wordmark header + account action, hero CTAs (Add listing / Browse categories), search+location bar, How-it-works step rail, **curated top-6 category grid** (no more chip wall), Featured rail; `ListingCard` `🖼️` → lucide `ImageOff`. AR mirrors correctly + matches the web reference; renders at 360dp; mobile typecheck 8/8. **Root-caused the white-screen** (fixed `tabBarStyle.height` / direct-lucide-in-`tabBarIcon` on Fabric crashed the navigator; render hero bg from Views not SVG; stale Metro watcher needed `--clear`). Remaining M7: rest of the screens + emoji, physical-device QA, consolidated comparison set. | 2026-08-06 |
| 27 | **M5 Android VERIFIED on-device + M6 config + production AAB.** Ran EAS dev build `157a241a` (SDK 57) → installed on `emulator-5554`, bundle loads (no Fabric regression). Consolidated on-device pass (signed in @ahmed): **deep links** listing/profile/https-locale route correctly, **authorized** conversation opens / **unauthorized** shows empty (RLS gate, no leak); **image chooser** (Take Photo/Library) + camera-permission + graceful denial, no mic; **push** permission fires on sign-in, soft-fails without FCM (0 device_tokens, non-fatal); **biometric** toggle correctly hidden w/o enrolled fingerprint (full lock-cycle → physical device, emulator FLAG_SECURE); **permission audit** clean (camera/notif/biometric only). **3 defects found+fixed:** `+native-intent` custom-scheme parsing (`justswap://listings/x` dropped `listings` → Unmatched Route) `39409f1`; `acquireImages` hung-promise on Alert dismiss + `BiometricLock` resume-flash `d6ca90c`. **App Links:** real signing SHA-256 (`7A:F9:…:E7`) → `assetlinks.json` `af6a2f4` → web redeployed live (200/json); `pm get-app-links` signature matches. **M6:** iOS privacyManifests + no-alpha `icon-ios.png` + versionCode/buildNumber `d6ca90c`; `docs/fcm-setup-checklist.md`; **Android production AAB validated** (`0aa6e4f3`, versionCode 1, signed w/ the `7A:F9…E7` upload key) — first attempt `91a29340` FAILED on `Cannot find module 'babel-preset-expo'` (transitive-only under pnpm isolated; release JS bundling needs it) → **fixed** by adding it as a direct devDep (`5f074e9`). **§7:** typecheck 8/8, diff --check clean, secret scan clean, translation parity web 647/647 + mobile 668/668, m4 13/13, m5 7/7. **No prod test data.** iOS signing/APNs/Team-ID/TestFlight + D-2 + FCM remain owner-gated. Commits `4035e31`,`d6ca90c`,`af6a2f4`,`39409f1`,`5f074e9` (+ tracker). | 2026-08-06 |
| 26 | **M4 rollout fixes + Phase M5 native integrations (Android-first).** **M4 finalize:** audited the live 0018 rollout — deployed web is NOT broken (writes go through the service-role API, which bypasses RLS; direct-Supabase writes are correctly gated); built the **web Terms-acceptance gate** (getApi 403-interceptor → EULA modal → POST /me/terms → retry) so redeploying the new API won't 403 web users; corrected the support contact to **support@justswap.me** repo-wide (bundle id `me.justswap.app` untouched); hardened Terms on the public **bio** (migration **0019** trigger + PATCH /me check); `/health` reports `moderation:{active:false}` honestly. **M5:** (A) native camera/library chooser reusing the upload pipeline + permissions, mic disabled; (B) native Share sheet (justswap.me links) on listing + profile; (C) deep links + Android App Links (autoVerify) + iOS AASA prep + web `/.well-known/{assetlinks.json,apple-app-site-association}` (verified served as JSON; SHA-256/Team-ID are documented placeholders) + `+native-intent` locale-strip; (D) opt-in biometric app-lock over the session (never stores the password; Sign-out escape); (E) push foundation — migration **0020** (device_tokens owner-only + push_outbox service-role + enqueue trigger), NestJS PushModule (register/revoke/list + outbox worker, MockProvider, cron OFF by default, admin process endpoint), mobile registration + PushManager. **Verified:** typecheck 8/8; expo-doctor clean (bar the benign react 18/19 dup); migrations 0019+0020 applied to live DB; **m4-trust-safety 13/13** + **m5-push 7/7** (reversible). New native modules → the pre-M5 dev client needs an EAS rebuild for the on-device pass. **Deployed web+API to Railway (each its own Dockerfile, context=repo root) + verified on prod: 12/12 behavioral smoke** (terms gate 403→accept→stamp→open, bio gate, push register/list/revoke, `/health` moderation:false, well-known JSON, `support@justswap.me`), throwaway-user cleanup clean. **Pending owner:** EAS rebuild; D-2 provider; FCM/APNs creds; App-Link SHA-256/Team-ID. Commits `0327363`,`7362b7e`,`9045b11`,`32988d9`,`bcdd70a`,`40a4fd1`,`7c44ee2`,`a71de6f`,`7df9167` (+ this deploy-status doc). | 2026-08-06 |
| 25 | **M4 — Trust & Safety + Apple 1.2 (built + verified; 4/5 pillars done, D-2 = owner call).** Scouted the trust-&-safety surface (7-agent workflow) + D-2 providers (4-agent, live 2026 pricing). **Shared/DB:** `TERMS_VERSION` (@swap/config); `Profile.terms_accepted_version/at`; migration **0018** (profiles cols + `current_terms_version()`/`has_accepted_current_terms()` + listings/messages/reports INSERT RLS re-defs + dup-pending-report unique index) mirrored into `full_setup.sql`. **API:** `TermsGuard` on listing/message/proposal/rating/image writes; `POST /me/terms` (server-stamped); `ReportsService` message/conversation **participant authz** + 409 dup handling; `ContentModerationService` seam (no-op, wired at listing-create); REST `acceptTerms`. **Mobile:** self-contained `ReportDialog`/`ReportSheet` (listing/user/message/conversation) + auth/error/dup states; block toggle on profile + chat ⋮ + `app/blocked.tsx`; `app/support.tsx` + `app/terms.tsx`; `TermsProvider.ensureAccepted()` gating all 9 UGC write sites + acceptance sheet; new i18n (`termsGate.*`, dup/participation strings, ar+en parity). **Web admin:** message/conversation report context (author + snippet + link; deleted-safe). **Docs:** `docs/moderation-sla.md` (24h SLA) + `docs/d2-content-moderation.md` (D-2 brief). **Verified:** typecheck 8/8; **migration 0018 applied to the live DB + verified**; **9/9 security tests** (`apps/api/test/m4-trust-safety.mjs`); consolidated live pass on `emulator-5554` (app renders; terms gate → accept → server-stamps `version=1` → write proceeds; Support + Profile nav render). All test artifacts reverted (demo terms reset to NULL). **Open:** D-2 proactive-filter provider (owner) + Google Play Child-Safety declaration/CSAE URL (Phase S). **Pushed** (`c5a5318`…`5dedf23`). | 2026-08-05 |
| 24 | **M3 CLOSEOUT — full cross-account live verification; M3 exit criterion MET.** One consolidated pass: EAS dev client as @ahmed (device 1) + a concurrent Node Supabase client as @khalid (device 2). Verified **email deep-links** (real `token_hash` tokens, LTR): signup-confirm → onboarding, resend, recovery → reset-password (+ `updateUser` happy-path via Node). **Owner-management** (@khalid, reversible): field edit / pause / delete / image add-remove-reorder. **Cross-account read-receipt:** Khalid's live message flipped `is_read` false→true under Ahmed's session (RLS-scoped). **Live Realtime chat:** `subscribeToMessages` delivered + auto-scrolled the message in Ahmed's open thread. **Live notification:** the `new_message` row incremented Ahmed's 🔔 badge 0→1 via `subscribeToNotifications` (no refresh), and opening Alerts cleared it. **Arabic-RTL spot-check:** Home renders fully mirrored (tab bar reversed, text right-aligned, chips RTL) after the boot guard flipped `forceRTL`+reloaded. Cleaned up all test artifacts (test message + notification deleted; 8 `@justswap-qa.dev` QA users deleted; app locale reset). Recorded backend `listings_count`-not-decremented + emulator `-gpu auto`/uiautomator dev-env notes. No new features; M4 not started. Batched commits; **not pushed (awaiting go-ahead).** | 2026-07-30 |
| 1 | **Mobile audit + release plan authored.** Read all of `apps/mobile` (single static screen, no nav/live-data/`eas.json`, SDK 51). 8-agent workflow: web-parity, package mobile-safety, UGC/safety-backend, + 5 store-requirement research agents. Verdict: ~3–5% to submission; shared layer ~90% reusable. Wrote this plan (M0–M7). No code changed. | 2026-07-22 |
| 2 | **Phase M0 — toolchain & live-data baseline DONE.** Upgraded Expo **SDK 51→57** (RN 0.86 / React 19.2.3, New Arch on) via `expo install --fix`; added AsyncStorage + `react-native-url-polyfill` + `react-native-get-random-values`. **Fixed `createSupabaseClient`** to accept a storage adapter (additive, non-breaking — web unaffected). Rewrote mobile `supabase.ts` (AsyncStorage, `detectSessionInUrl:false`, non-null, AppState refresh) + new `api.ts` (`SwapApiClient` + `getToken`); polyfills at app entry; `app.json` → name "JustSwap", `me.justswap.app`, scheme `justswap`, removed `newArchEnabled`, migrated splash to the `expo-splash-screen` plugin; `.env`/`.env.example`; tsconfig `baseUrl` removed (TS 6.0 deprecation). Replaced the static home with an **M0 connectivity harness**. Resolved D-4; D-5 left open (no company assumed); D-2 deferred to M4. Reordered this plan (build-first; accounts/store → Phase S). **Typecheck 8/8 green, expo-doctor 19/20.** On-device smoke still to run (Android). | 2026-07-22 |
| 3 | **Phase M0 build bring-up (Android local dev build).** Set ANDROID_HOME/PATH (User scope); installed NDK 27.1.12297006 / CMake 3.22.1 / platform-36 / build-tools 36; booted the Pixel_3a API-34 emulator from the terminal; started the local NestJS API (port 4000). Added `expo-dev-client`, ran `expo prebuild` (android/ + ios/ gitignored), limited the emulator build to x86_64. **Diagnosed + fixed** a pnpm path-length build failure via **`nodeLinker: hoisted`** (deep `.pnpm` paths blew past CMake's object-path limit) — all third-party native modules then compiled. Added an env-gated headless auto-smoke to the harness. **Hit a hard wall:** the New-Arch app-level codegen link produces a **373-char object path** > Windows' 260 limit; the fix (`LongPathsEnabled`) needs an elevated registry write that is **denied** to me. Typecheck 8/8 green under hoisted. **M0 on-device smoke pending the long-paths enablement (human/admin — see Known gaps).** | 2026-07-22 |
| 4 | **Phase M0 CLOSED — verified on the Android emulator.** User enabled `LongPathsEnabled=1` (elevated); the build STILL failed at the New-Arch codegen link (`ninja: Filename longer than 260`) because the CMake-bundled **ninja 1.10.2 isn't long-path-aware** → **upgraded ninja to 1.12.1** (original saved as `ninja.exe.orig`). `expo run:android` then **BUILD SUCCESSFUL**; app installed + launched on `emulator-5554`. **M0 smoke 5/5 green:** auth `signin=ok`; **persistence across cold restart** `boot-session=ahmed@swap.demo`; RLS `count=6`; `realtime=SUBSCRIBED`; REST `/me` `user=@ahmed`. Reverted the temporary auto-smoke env vars. **M0 exit criterion MET.** | 2026-07-22 |
| 5 | **Phase M1 — navigation shell + i18n/RTL + core components (verified on emulator).** expo-router **Tabs** (Home/Browse/Messages/Notifications/Profile — emoji icons, themed, localized); **JS-only i18n** via `Intl` device-locale (ar/en; replaced hardcoded `'ar'`; `allowRTL`); dark theme from `@swap/config`; components **Screen/ListingCard/CategoryGrid/EmptyState**; real **Home** tab (categories + live featured listings via `@swap/api`); M0 harness → `/m0-check` (root anchored on `(tabs)` via `unstable_settings`). Typecheck 8/8; **rendered + screenshotted on emulator-5554** (Home + 5-tab bar, dark theme, en locale). Commit `fc05058`. Stubs: Browse/Messages/Notifications/Profile (M2/M3). | 2026-07-22 |
| 6 | **nodeLinker verification (Correction 1) → reverted to `isolated`.** Confirmed `nodeLinker: hoisted` was unnecessary: the Windows path wall is fixed by **ninja 1.12.1** alone — the isolated mobile build cleared every path error and only hit a clang **OOM** (low free RAM + 12-way parallelism), fixed by capping native parallelism (`org.gradle.parallel=false`, `workers.max=4`) → **BUILD SUCCESSFUL** (app-debug.apk 88 MB). **Reverted `nodeLinker`** to pnpm's strict default `isolated` (phantom-dep-safe; apps/web+api deploy from this repo). Verified under isolated: web `next build` ✓, api `tsc` ✓, api `ts-node` runtime (all modules resolve, `/health`+routes 200) ✓ → no phantom deps. `pnpm-lock.yaml` unchanged. Pushed all commits (backup). | 2026-07-22 |
| 7 | **Correction 2 — RTL-first i18n foundation (verified on device).** Locale fallback fixed to **ar** (Arabic-first) for non-ar/en devices. **Ported the full web `next-intl` catalog** (`src/i18n/{ar,en}.json`, 32 namespaces) into new `src/i18n/index.ts` (nested `t()` + `{param}` interpolation + `ar` fallback + `EXPO_PUBLIC_LOCALE` override) + a `mobile` namespace; rewired tab screens/components to catalog keys; removed the stub `src/i18n.ts`. Implemented **`forceRTL`**. **Verified on emulator:** forced `ar` → relaunch → **full RTL** (header/hero right-aligned, category chips RTL, **tab bar reversed** — screenshot). Typecheck 8/8. Production RTL-flip reload (`expo-updates`) + `expo-localization` = follow-ups. | 2026-07-22 |
| 8 | **Correction 2c — web-component inventory + RN primitive kit (RTL-verified) + expo-localization.** Ran a 5-agent workflow inventorying **all ~130 `apps/web` UI components** (PORT/ADAPT/SKIP + implied primitives) — caught real gaps in the ad-hoc list (no Button/Card/Input/Select/Badge primitives, `SwapPair`, `ItemArtwork`, `SegmentedControl`, `Avatar`). **Swapped `Intl`→`expo-localization` `getLocales()`** (native rebuild via `gradlew`, autolinked — `nodeLinker` stays isolated). **Built the primitive layer** `src/components/ui/`: `Button`, `Card`, `Avatar`, `Badge` (+tone maps), `RatingStars`, `Chip`, `Divider`, `StatCell`; refactored `CategoryGrid`→`Chip` and `ListingCard`→`Card`+`Badge`. Typecheck 8/8; **verified on emulator in LTR (device `en`) and RTL (forced `ar`)** — screenshots. Committed + pushed. Remaining kit (Select/BottomSheet, icon system, composites) tracked in the M1 checklist. | 2026-07-22 |
| 9 | **2c batch 2 — icon system + Select/BottomSheet + form primitives (RTL+LTR verified).** Installed `react-native-svg` + `lucide-react-native` (native rebuild via `gradlew`, autolinked; `nodeLinker` still isolated). Built `Icon`, `IconButton`, **`BottomSheet`** (RN Modal), **`Select`** (BottomSheet-backed — the heaviest form primitive), **`Input`/`Textarea`/`Checkbox`** — all RTL-safe (logical spacing; directional icons take a `mirror` prop). Added a **`/ui-kit`** dev preview route (`justswap://ui-kit`) exercising every primitive. Typecheck 8/8; **verified on emulator in LTR and RTL** — the Select opens its BottomSheet, lucide icons render, the whole kit mirrors (screenshots). Committed + pushed. | 2026-07-23 |
| 10 | **RTL strategy locked + `reloadAsync` verified on-device + first-launch analysis (D-7).** 5-agent research workflow (Fabric `direction`-prop on RN 0.86, New-Arch restart reports, capability loss, JS-RTL libs) + a primitive-conversion audit → **decision: keep the native `forceRTL` flag** (JS-managed `direction` silently breaks native-stack nav / horizontal scroll / TextInput caret with no JS remedy; the 18 primitives were never the cost). Installed `expo-updates`; built a `/rtl-verify` harness; **empirically verified `Updates.reloadAsync()` re-applies the flag on RN 0.86 Android both directions + across a genuine cold start** (screenshots) → **Option A** approved for the M3 switcher (iOS reload unverified till M5). Found the **iOS `CFBundleLocalizations` gap**; worked out the **first-launch mismatch** options (user to pick — D-7). Fixed a recurring clang OOM with a **CMake job pool**. `RatingStars` comment corrected (leading-edge auto-flip is intended). Typecheck 8/8; scaffolding reverted. | 2026-07-23 |
| 11 | **RTL direction invariant IMPLEMENTED + verified (D-7 resolved).** User set the hard invariant — a mismatched direction must never render, not even one frame — and finalized the locale rule (Arabic device ⇒ Arabic, any other language ⇒ English). Built the **Android native early-set** (config plugin `withAndroidRtlEarlySet` → `MainApplication.onCreate` forces `forceRTL(device-lang=="ar")` **before React loads**) + a splash-gated **JS boot guard** (`_layout.tsx`); `i18n` stops mutating `I18nManager`; iOS `CFBundleLocalizations:['ar','en']` base `en`. **Discovered** `Updates.reloadAsync()` throws `ERR_UPDATES_DISABLED` in a bare production release (needs EAS Update) → chose the native early-set (no reload, no account) over a reload. **Verified on-device (RN 0.86 Android):** `ar`→RTL, `en`→LTR, **Urdu→LTR English** (the override), all with **no reload** (`bundle-loads=1`). Fixed a CMake config-eval fail (plugin must `require('expo/config-plugins')`). Typecheck 8/8. Committed + pushed. | 2026-07-23 |
| 12 | **M1 component kit COMPLETE — ~22 components across 4 batches, all verified LTR+RTL on device.** Ported from the web inventory, RTL-first: primitives (`Skeleton`/`SegmentedControl`/`Accordion`/`ListRow`/`StrengthMeter`/`CategoryIcon`) + composites (`SwapPair`, `ItemArtwork`, `FeaturedCard`, `WantedCard`, `CategoryCard`/`Carousel`, `SellerCard`, `ProfileHeader`, `ChatBubble`, `ConversationCard`, `NotificationBell`, `FollowButton`, `MessageButton`, `ReportDialog`, `AvatarUpload`). Each exercised in `/ui-kit` + screenshot-verified both directions (ChatBubble own-bubble flips left in RTL; ConversationCard/ProfileHeader mirror; CategoryCarousel anchors from the right; report BottomSheet opens with its Select). **Fixed** `Icon` `mirror` (svg-style `scaleX` unreliable on Fabric → wrap a `View`; fixes all directional icons). **Deferred `ProposalContextCard`** to M3 (585-line stateful feature, not a kit shell). Typecheck 8/8. Committed in batches 3–6 (`3993e9d` + b76b172/c0dc383/ce16675 — pushing behind a degraded network). | 2026-07-23 |
| 13 | **M2 read surface — Browse + Listing detail + Public profile (live data, RTL+LTR verified).** `Browse` tab: debounced search + category chips + Newest/Most-viewed sort + paginated `getListings`. `app/listings/[id]`: image pager + category/condition/city + `WantedCard` + description + `SellerCard` + Save (`saveListing`)/Message/Report + `incrementListingView`. `app/users/[username]`: `ProfileHeader` + their listings + received reviews. `ListingCard`/Home cards → detail; Intl-free `monthYear` helper; self-contained `mobile.browse`/`mobile.detail` keys (incl. condition labels). Verified on-device against **live Supabase data**, both directions — browse → open a listing (view count incremented to 81) → seller `@khalid`'s public profile (4 listings, 5.0 rating). Category chips + image pager anchor from the right in RTL (native flag). Typecheck 8/8. Commit `91cd8ec`. | 2026-07-23 |
| 14 | **M2 remainder — own-Profile tab + Saved + Home category filter (live data); M2 exit criterion MET.** Profile tab is auth-reactive (`supabase.auth.getSession` + `onAuthStateChange` + `signOut`): signed-in → `ProfileHeader` + Saved/Sign-out + My listings (`getListings` ownerId); signed-out → sign-in prompt (real auth = M3). Saved screen (`app/saved`, `getSavedListings`). Home category chips → filtered Browse (`categoryId` param). Scouted the surface with a 4-agent workflow (web own-profile/saved/following + mobile auth pattern + API/i18n gaps). **Verified on-device against the persisted M0 session (@ahmed):** real profile (7 listings, 4 followers) + real saved items (PS5, Nissan Patrol) + category→filtered Browse. **Following feed deferred** (scaffolded-but-unmounted on web). Typecheck 8/8. Commit `3002a39`. | 2026-07-23 |
| 15 | **M3 auth — Login + Register (real supabase auth), verified on-device.** `app/login`: email-or-username (username→email via `email_for_username` RPC) + password → `signInWithPassword`; handles `email_not_confirmed`. `app/register`: full form (name/username/country+city/email/phone/password/confirm/terms) → `signUp` with `options.data` metadata; the profiles row is created by the DB `handle_new_user` trigger (app does NOT insert it); email confirmation ON → "check your email" screen. Profile "Sign in" → `/login`. Intl-free `buildPhone`. Scouted with a 3-agent workflow (web auth/onboarding + supabase methods + profile creation + i18n). **Verified:** signed out → logged in as @ahmed via the new screen → Profile signed-in; register renders all fields. Typecheck 8/8. Committed **locally** `c380945` (batching pushes per request — nothing pushed). | 2026-07-23 |
| 19 | **M3 create-listing (code + native link done); on-device blocked by a Fabric render regression.** Built `app/new-listing` (full create form) + `src/lib/upload` (expo-image-picker → sign/`uploadToSignedUrl`(base64→bytes)/`addListingImage` — exact web pipeline) + "List an item" Profile entry; added `expo-image-picker`/`base64-arraybuffer` + the image-picker config plugin (`58a7ca8`). **Native saga:** pnpm's isolated linker won't autolink a *new* native module incrementally (detected but not built as a subproject) → required a full `expo prebuild` + rebuild; the prebuild **clears android/** (wipes machine-local tuning — re-applied CMake job-pool + `gradle.properties` workers=2/x86_64 + `local.properties` from backups; RTL early-set re-applied by its plugin). Gradle daemon OOM'd once (emulator + daemon RAM) → kill emulator + workers=2 fixed it. After rebuild the module links (ExpoCropImageActivity in manifest, no missing-module error). **⚠ BLOCKER:** post-prebuild the app **renders blank** — React reconciles (`[boot] ready=true`) but **no views mount** (raw `<View>` blank; uiautomator hierarchy empty), whole-app not feature-specific. Not a screencap/GPU artifact (uiautomator agrees). `expo install --check` shows ~9 packages behind expected (screens/updates/router) — a lead, though the app rendered on these same versions pre-prebuild, so the **prebuild-regenerated native project** is the likelier differentiator. Also hit (and documented) dev-loop gotchas: **CI-mode Metro freezes the bundle** (use watch mode), the **adb-reverse `tcp:8081` tunnel** must be re-set after an emulator reboot, and the boot-guard's dev `reloadAsync` can hang ("react activity isn't active"). Typecheck 8/8. Committed **locally** `58a7ca8`. | 2026-07-23 |
| 21 | **M3 deal closing + post-swap rating, verified cross-account in the EAS Android development client.** Extended the existing `ProposalContextCard`; no new surface. In `agreed` / `awaiting_confirmation`, each party can pick one received-item photo with the proven picker → sign → `uploadToSignedUrl` (base64→bytes) pipeline, then `confirmSwap`; private `swap-confirmations` read via `getConfirmations` supplies **both** signed photos to both parties. The server owns the atomic completion transition. “Something went wrong” opens a reason sheet and calls `disputeSwap`, which creates the admin report; the action was surfaced and backend contract mirrored without creating a test dispute. On completion, a non-blocking 1–5-star + optional-text rating sheet auto-opens, supports later rate/edit, and persists through `rateProposal`. **Live run:** @ahmed proposed `Cloud_Build_Verification` to @khalid, Khalid accepted and confirmed a picked photo, Ahmed saw it, confirmed a picked photo, and the proposal completed; both photos were mutually visible to each role. Ahmed submitted 5★ + text and Khalid submitted 4★. Verified LTR, then changed the Android **app locale natively** to `ar-SA`, cold-launched (native early-set kept the first rendered app frame RTL), checked the Arabic completed/deal/rating layout, and restored `en-US` the same way. Confirmed the CNG-generated `MainApplication.kt` still contains the D-7 `I18nUtil.forceRTL` early-set before React loads. Full typecheck passed; no push. **Next requested M3 items: mobile notifications center/badge and follow/unfollow.** | 2026-07-25 |
| 22 | **M3 web-parity reconciliation + remaining feature code.** Audited web as authority: mutual confirmation photos, proposal transitions, and independent/editable ratings already matched; mobile now refreshes confirmation photos immediately and mirrors web's 2.6s completion celebration before the optional rating prompt. Replaced mobile copies of shared rules (`MAX_PROPOSAL_ITEMS`, image cap); rebuilt create-listing as web's 3-step phone-width flow with live exchange preview/safety note; added browse filters, real optimistic follow/unfollow, profile edit/avatar, read receipts/chat safety/error, owner listing edit/pause/delete/immediate image add/remove/reorder, native signup/resend/recovery callback + onboarding/reset, and the live notification center/tab badge. Fixed a shared Realtime helper defect discovered on-device: badge+screen subscribers had reused a channel name, so each call now allocates its own channel. **EAS dev client:** English/LTR wizard rendered; real notifications rendered and badge cleared on opening; native `ar-SA` cold boot rendered Arabic/RTL, then restored `en-US`. **Live two-account social pass:** Khalid unfollowed then followed Ahmed; Ahmed received the new-follower row and badge, which cleared on entry. Full typecheck + diff check passed. **Not yet live-E2E:** management mutations, read receipt, and email callback/recovery — the latter awaits Supabase's `justswap://auth/callback` redirect allow-list. No push. | 2026-07-25 |
| 23 | **Partial M3 live-QA, then superseded by a new workstream.** Supabase Redirect URLs now include `justswap://**`. The installed EAS development client opened native Register and the country/city selectors under the configured callback path; a disposable-mail confirmation run was started but deliberately stopped before submitting the account, so it produced no completed auth result. **Owner pass:** @khalid opened an existing listing's Edit Listing route; persisted images and management controls loaded, then a real pause → save → reload-as-paused → reactivate → save → reload-as-live mutation round-trip passed. Listing field edit/delete/image add-remove-reorder, cross-account read receipt, email links, and the required two-device lifecycle pass remain. No code, commit, or push. | 2026-07-25 |
| 20 | **EAS cloud Android development build VERIFIED; Fabric blocker resolved.** Authenticated as `most.othman`; created and linked `@most.othman/justswap` (`9f8660e2-f233-4eeb-a578-776367ebec8e`), ran `eas build:configure -p android`, preserved the Android native RTL early-set config plugin, and built Android development APK `66e95688-e3de-48e1-ae29-db3a917265fc`. EAS Gradle build succeeded (including `react-native-screens`); its APK was installed on `emulator-5554` and connected to watch-mode Metro with `adb reverse tcp:8081 tcp:8081`. The full Fabric UI rendered normally. Verified the complete create path as @ahmed: system picker → one image → title/category/country/Riyadh/description/open exchange → publish → `Cloud_Build_Verification` detail. After a cold dev-client restart, it remained first in My listings. This isolates the blank render to the Windows-local native path. Typecheck passed before submission; no push (per batching request). | 2026-07-25 |
| 18 | **M3 forgot-password request screen + login link, verified on-device.** `app/forgot-password`: email → `supabase.auth.resetPasswordForEmail` (no explicit redirect — uses the project Site URL = web reset flow, matching register); always shows the "sent" state (MailCheck) — never reveals whether an email is registered; disabled send until an email is entered. Login gained a "Forgot password?" link (`Pressable` + `hitSlop` for a real touch target — a bare `Text` onPress was mis-hitting) → `/forgot-password`, route registered in the root Stack. The fully in-app **reset** (deep link → set new password) is M5; today the emailed link resolves on the web reset page. **Verified:** login link navigates → type email enables Send → Send shows the sent state, no error. Typecheck 8/8. Committed **locally** `309e62c`. | 2026-07-23 |
| 17 | **M3 swap proposal — negotiation loop, verified cross-account on-device.** Built `ProposeSwapSheet` + `ListingPicker` (multi-select of your own active listings, `MAX_PROPOSAL_ITEMS` cap, safety hint, note) → `api.createProposal` → route into the conversation; listing-detail gained a "Propose a swap" primary CTA (two-row sticky bar; hidden on your own listing). Built `ProposalContextCard` pinned above the thread: role-flipped give/get, status `Badge`, turn banner, turn-gated actions — Accept/Counter/Decline (your turn) · Withdraw (still live); Counter reuses `ListingPicker` over the *proposer's* items; live status via `subscribeToProposal`. Deal-closing (photo confirm/dispute/rating) deferred to M5 (needs `expo-image-picker`) — card shows the banner for those states. Scouted with an Explore agent (web ProposalContextCard/ProposeSwapDrawer + @swap/api + types + i18n). **Verified (@ahmed→@sara):** create 2-item proposal (Pending; proposer sees Waiting+Withdraw) → sign in as recipient (Accept/Counter/Decline, correct give/get flip) → counter with a 3-item bundle → status flips Countered + turn flips back. Inbox badges render (Pending amber / Countered blue). Typecheck 8/8. Committed **locally** `42653b2` (still batching pushes). *(Emulator is now signed in as @sara — a test artifact; re-login as @ahmed if needed.)* | 2026-07-23 |
| 16 | **M3 messaging — inbox + Realtime conversation, verified on-device.** `app/(tabs)/messages`: auth-reactive inbox built client-side via `src/lib/chat.ts#fetchConversations` (participants → conversations desc → per-conv other-user/last-message/unread/proposal-status), `ConversationCard` rows, refetch on focus, signed-out prompt. `app/messages/[id]`: live `getMessages`, own/other `ChatBubble`s, `subscribeToMessages` Realtime INSERT (dedupe vs optimistic add), `send()` = `sendMessage` + optimistic append + restore-on-fail + clear. Listing-detail `MessageButton` → `getOrCreateConversation` → thread (→ `/login` if signed out). Added Intl-free `timeAgo()`. Fixed the Android composer-behind-keyboard bug (`KeyboardAvoidingView behavior="padding"`). **Verified (@ahmed):** 4 real conversations render (Arabic names + last message + unread badge); opening Khalid's thread shows correct own(green/right)/other bubbles; sending appends a "now" green bubble and clears the composer. Typecheck 8/8. Committed **locally** `f49da97` (still batching pushes — nothing pushed). | 2026-07-23 |

---

## ⚠ The one hard stop — when you must register a developer account (VERIFIED 2026-07-22)

You asked me to verify exactly where local building/testing breaks. Confirmed:

- **M0 → M4 need NO developer accounts, on either platform.** All code + config builds locally. On this **Windows** machine you'll validate on **Android** (emulator or device via `pnpm mobile` / a dev build). iOS JS-only screens can optionally be smoke-tested on a physical iPhone via **Expo Go** (free), but a Mac is needed for the iOS Simulator — so Android is the practical local target through M4.
- **Android is fully testable locally the ENTIRE way (M0–M6),** with no paid account: dev builds, sideloaded APK/AAB, and **FCM push via a free Firebase project**. The **Google Play account ($25 one-time)** is needed only to *distribute* — Phase S (internal-testing track / production).
- **iOS breaks at M5. This is the register moment.** iOS **push notifications require APNs credentials, which Apple will not issue without a paid Apple Developer membership ($99/yr)**, and building for a **real iPhone / TestFlight** requires distribution signing that also needs the paid account. (Verified against Expo's push-setup docs: on the first EAS iOS build you're prompted to enable push and generate the APNs key, which requires the paid account.) On Windows there is no local iOS build at all, so in practice **the paid Apple account gates every iOS on-device validation** — it is the single unavoidable prerequisite for the iOS half of M5–M6.

**Net:** build M0–M6 locally and validate on Android with zero accounts. **Register the Apple Developer account when you reach M5** (to test iOS push and produce an installable iOS build). **Register Google Play at Phase S** (to publish). Everything else account/legal/store-related is consolidated in **Phase S**.

The `── HARD STOP ──` line below marks the M4→M5 boundary in the checklist.

---

## Phase Checklist

Legend: `[x]` done · `[~]` partial · `[ ]` not started · `[!]` blocked-on-human · tags: `(me)` I can complete with no account · `(me+backend)` also needs a small backend/DB change · `(you)` human-only.

---

### Phase M0 — Toolchain & Live-Data Baseline ✅ DONE `(me)`
> Make the skeleton a real, authenticated app talking to the live backend. (Session 2.)

- [x] **Upgrade Expo SDK 51 → 57** (RN 0.74→0.86, React 18→19.2.3). New Architecture is on (mandatory ≥ SDK 55); removed the `newArchEnabled` flag. `expo-doctor` 19/20.
- [x] **Lock app identity:** `name: "JustSwap"`, `slug/scheme: "justswap"`, `ios.bundleIdentifier` + `android.package` = **`me.justswap.app`** (immutable after first store submit — locked before any store records exist). Universal-Links/App-Links domain will be **justswap.me** (M5).
- [x] Added RN companions: `@react-native-async-storage/async-storage`, `react-native-url-polyfill`, `react-native-get-random-values`; imported `react-native-url-polyfill/auto` + `react-native-get-random-values` at app entry (`app/_layout.tsx`) so REST query strings + crypto work on native.
- [x] **Fixed the storage-adapter gap** in `packages/api/src/client.ts` — `createSupabaseClient(url, anonKey, opts?)` now accepts `{ storage, detectSessionInUrl, autoRefreshToken, persistSession }`. Additive; web (`@supabase/ssr`) unaffected; all 8 workspaces still typecheck.
- [x] Rewrote `apps/mobile/src/lib/supabase.ts`: injects `AsyncStorage`, `detectSessionInUrl:false`; reads `EXPO_PUBLIC_SUPABASE_*`; **throws loudly if unconfigured** (was silently `null`); AppState-driven `startAutoRefresh`/`stopAutoRefresh`.
- [x] Added `apps/mobile/src/lib/api.ts`: a shared `SwapApiClient` whose `getToken()` returns the live Supabase access token.
- [x] `.env` (real live creds — git-ignored) + committed `.env.example`. The anon key is the public publishable key; API base defaults to `http://10.0.2.2:4000/api/v1` (Android-emulator→host).
- [x] Migrated splash to the `expo-splash-screen` plugin; removed the deprecated top-level `splash`; removed tsconfig `baseUrl` (TS 6.0 deprecation). Metro monorepo config cleaned (dropped `disableHierarchicalLookup`).
- [x] CI/turbo: mobile is already covered by `turbo typecheck` (it has a `typecheck` script) and CI runs `pnpm typecheck`. No change needed. *(Mobile has no `dev`/`build` task, so it's correctly skipped by `turbo dev`/`build`.)*
- [x] Temporary **M0 connectivity-harness** home screen exercising auth + persistence + an RLS read + a REST call + Realtime (replaced by real screens from M1).

**Exit criterion:** ✅ **MET (verified on `emulator-5554`, 2026-07-22).** Local Android dev build (`expo run:android`) succeeds; the app boots, **signs in against live Supabase**, **session persists across a cold restart** (AsyncStorage), and an RLS read (6 listings), a Realtime subscription, and an authenticated REST `/me` all succeed. Windows path wall cleared by `LongPathsEnabled` + ninja 1.12.1 (see Known gaps).

---

### Phase M1 — Navigation, i18n/RTL & RN Component Kit `(me)`
- [~] **`expo-router` tabs** (Home · Browse · Messages · Notifications · Profile) — DONE (`app/(tabs)/`, emoji icons, themed, localized labels; root anchored on `(tabs)` via `unstable_settings`). Nested stacks + auth-screens-outside-tabs land with M2/M3.
- [x] **i18n + RTL** — DONE + **verified on device both directions** (Correction 2): device locale via **`expo-localization` `getLocales()`** (reliable cross-Android; replaced Hermes `Intl`) with **Arabic-first `ar` fallback** (non-ar/en → ar); the **full web `next-intl` catalog ported** (`src/i18n/{ar,en}.json`, 32 namespaces) + a `mobile` namespace, loaded by `src/i18n/index.ts` (nested `t()` + `{param}` interpolation + `EXPO_PUBLIC_LOCALE` test override); **`forceRTL`** — an Arabic UI mirrors to RTL even on an LTR device (screenshot-confirmed: header/hero right-aligned, chips RTL, tab bar reversed; `en` device → LTR). **RTL DECIDED + IMPLEMENTED (Sessions 10–11 — see D-7):** native `forceRTL` flag (not JS `direction`). **Locale rule:** Arabic device ⇒ Arabic/RTL, any other language ⇒ English/LTR. **Hard invariant — a mismatched direction never renders (not one frame):** an **Android native early-set** (`plugins/withAndroidRtlEarlySet.js` → `MainApplication.onCreate` forces the flag from the device locale *before* React loads) + a splash-gated **JS boot guard** (`app/_layout.tsx`) as backstop. Verified on-device: `ar`→RTL, `en`/**Urdu**→LTR, all with **no reload** (early-set pre-aligns). iOS aligned by `CFBundleLocalizations:[ar,en]` base `en` (UNVERIFIED till M5). Note: `Updates.reloadAsync()` throws in a bare production release (needs EAS Update), which is why the boot path is native, not a reload. TODO: ICU plural forms; M3 in-app switcher restart mechanism.
- [x] **Theme** from shared `@swap/config` tokens (dark navy; `src/theme.ts`). *(Note: `@swap/config` ships a single dark palette — a light/dark toggle would need light tokens added there; deferred.)*
- [x] **RN component kit — DONE (~22 components, all verified RTL+LTR on device via the `/ui-kit` route).** Derived from a full `apps/web` inventory (5-agent workflow; ~130 web components mapped). **Primitives:** `Button`, `Card`, `Avatar`, `Badge` (+`PROPOSAL_STATUS_TONE`/`STATUS_TONE`), `RatingStars`, `Chip`, `Divider`, `StatCell`, `Icon`/`IconButton` (lucide + react-native-svg; `mirror` flips via a **wrapping `View`** — the svg-style transform is unreliable on Fabric and drops the glyph), `BottomSheet` (RN Modal), `Select` (BottomSheet-backed), `Input`/`Textarea`/`Checkbox`, `Skeleton`, `SegmentedControl`, `Accordion`, `ListRow`, `StrengthMeter`, `CategoryIcon`. **Composites:** `SwapPair` (signature ⇄), `ItemArtwork`, `FeaturedCard`, `WantedCard`, `CategoryCard`/`CategoryCarousel`, `SellerCard`, `ProfileHeader`, `ChatBubble`, `ConversationCard`, `NotificationBell`, `FollowButton`, `MessageButton`, `ReportDialog`, `AvatarUpload`. `Screen`/`EmptyState` present; `ListingCard`/`CategoryGrid` refactored onto the primitives. Reuses `@swap/ui` formatters. **Deferred: `ProposalContextCard`** — a 585-line stateful *feature* (proposal state machine + Realtime + counter/confirm/dispute sheets + rating/celebration); build it with the **M3 swap/chat flow** against real data, not as a hollow kit shell. **Later polish:** gradient `ItemArtwork` placeholder (`expo-linear-gradient`); gesture-driven `@gorhom` sheet; real image picker for `AvatarUpload` (`expo-image-picker`). Dev preview: `justswap://ui-kit`.
- [x] **Decision D-1 confirmed:** browse uses the direct `@swap/api` RLS query layer (`getListings`) — Home already does — not the REST browse endpoint (which skips block-filtering).

**Exit criterion:** Navigable shell, every route reachable (screens may be stubbed), Arabic RTL correct end-to-end, theme applied. **Nav shell + Home verified on `emulator-5554` (2026-07-22); full RTL + remaining screens ongoing.**

---

### Phase M2 — Feature Parity: Browse / Read Surface `(me)`
> All read/discovery/profile surfaces against live data. Shared API for every item already exists.

- [~] **M2 EXIT CRITERION MET (Sessions 13–14, verified on-device against live data, both locales):** Home (featured + categories → filtered Browse) ✅ · Browse (search + category filter + sort, paginated) ✅ · Listing detail + view tracking ✅ · **Save + Saved screen** ✅ · **own-Profile tab** (auth-reactive via `supabase.auth`; signed-in → `ProfileHeader` + My listings + Saved/Sign-out) ✅ · **public profile** (listings + reviews) ✅. **Deferred/secondary:** Following feed (scaffolded-but-unmounted on web too) · a dedicated Categories index (covered by Home chips + the Browse filter) · static legal/info screens (link the hosted URLs — Phase S) · profile-edit + real `expo-image-picker` (→ M3).
- [ ] Static legal/info screens (terms/privacy/safety/support/disclaimer) — link to the hosted URLs (Phase S).

**Exit criterion:** Signed-in user browses → opens a listing → saves it → views a seller's public profile & reviews, live data, both locales.

---

### Phase M3 — Feature Parity: Auth + Transactional Loop `(me)`
> Core barter lifecycle + all writes. `expo-image-picker` is linked and verified in the EAS development client for listing and confirmation uploads.

- [x] Auth: **login/logout/register/forgot request** ✅ (prior on-device verification). Session 22 adds the web-equivalent unconfirmed-email resend, `emailRedirectTo` to `app/auth/callback`, native OTP verification → onboarding/recovery reset, `app/onboarding` (name/country/city/avatar), `app/reset-password`, and profile edit/avatar. **End-to-end email verification DONE (Session 24):** live signup-confirm, resend, and recovery mail links (real `token_hash` tokens) each opened `app/auth/callback` → `verifyOtp` → routed to native onboarding / reset-password; `updateUser` happy-path (new password) confirmed via Node (it rejects a same-as-current password by design). LTR verified; boot guard keeps direction correct. *(adb deep-link gotcha: single-quote the URL inside `adb shell` or the shell truncates it at the first `&`.)*
- [x] Create + manage listing: image upload/publish **verified on-device (Session 20)**. Session 22 ports web's three-step create flow (photos/title/category → condition/location/description → wanted/live exchange preview/safety), imports `FREE_PLAN_MAX_IMAGES` rather than copying it, and adds the owner-only edit/pause/delete + immediate API-backed image add/remove/reorder route. The wizard rendered in the EAS dev client; pause → save → reload → reactivate → save → reload passed live (Session 23). **Full owner-management pass DONE (Session 24, @khalid, reversible):** field edit (title/category/condition/country/city/description/wanted/pause) via `updateListing`, delete via `deleteListing`, and image add/remove/reorder — all persisted and reverted. *(Backend note: the denormalized `profiles.listings_count` stat is not decremented on a soft-delete — a DB-trigger nuance, not a mobile bug.)*
- [x] Messaging **inbox** ✅ + **Realtime chat** ✅ + **start conversation from a listing** ✅ (prior live verification). Session 22 adds the web-equivalent RLS read receipt on open plus compact safety/send-error surfaces. **Two-account live check DONE (Session 24):** a concurrent Node client (@khalid) sent a live message → it arrived in @ahmed's open thread via `subscribeToMessages` (list auto-scrolled to it) and @ahmed's app flipped that message's `is_read` false→true — cross-account, RLS-scoped read receipt. *(Report-a-message remains M4.)*
- [~] Proposals: propose (bundle) ✅ → counter ✅ → accept ✅ / decline ✅ / withdraw ✅ — the **negotiation loop** is built + verified on-device (ProposeSwapSheet + ListingPicker, ProposalContextCard pinned in the conversation with role-flipped give/get, status badge, turn banner, and turn-gated actions; live status via `subscribeToProposal`). Mutations go through the shared REST `SwapApiClient` (`createProposal`/`counterProposal`/`acceptProposal`/`declineProposal`/`cancelProposal`).
- [x] Deal closing: both-sides photo confirm → complete; dispute. `ProposalContextCard` now exposes the real `agreed` / `awaiting_confirmation` actions: system picker → signed `swap-confirmations` upload → `confirmSwap`; the server's atomic RPC completes after the second confirmation. Both parties load the two signed proof photos; “Something went wrong” collects an optional reason → `disputeSwap` / admin report. **Verified live cross-account in the EAS development client (Session 21).**
- [x] Post-swap rating/review: completion auto-opens a dismissible 1–5-star + optional-text sheet; completed card offers rate later/edit. Uses `rateProposal` and `getMyRatingForProposal`; **both parties submitted independent live ratings (Session 21).**
- [x] Notifications center + unread badge: direct-RLS `getNotifications`/mark-all-read + Realtime screen, plus tab badge. EAS dev client loaded real rows, cleared the badge on entry, and Ahmed received Khalid's real new-follower notification (Session 22). The shared subscription helper now makes a unique channel per consumer, so tab badge + center coexist.
- [x] Follow/unfollow: listing seller card + public profile load `isFollowing` and perform optimistic `followUser`/`unfollowUser` with rollback. **Live cross-account verified:** Khalid unfollowed/followed Ahmed → Ahmed's alert/badge. The web Following-feed component/helper is present but **not mounted on the web home**, so no mobile following feed was invented.

**Exit criterion:** ✅ **MET (Session 24, 2026-07-30).** The cross-account full lifecycle (create → propose → chat → accept → both confirm → completed → rate) passed in the EAS dev client (Sessions 21–22). Session 24 closed the remaining gaps in one consolidated cross-account pass — app = @ahmed on the EAS dev client + a concurrent Node client = @khalid as the second device: live signup-confirm/resend/recovery email deep-links, cross-account read-receipt, remaining owner listing management, live Realtime chat, and **live notifications** (🔔 badge increment on delivery + clear on read). Verified LTR with an Arabic-RTL spot-check (Home fully mirrored). A literal two-**physical**-device pass is retained for Phase-S final release QA.

---

### Phase M4 — Trust & Safety Parity + **Apple Guideline 1.2** `(me+backend)`
> Highest store-rejection risk. Build to the stricter of Apple/Google. Backend has reporting + blocking; it lacks proactive filtering + a persisted ToS gate; none of it is in a mobile UI. The **EULA legal text** is hosted in Phase S — here we build the *mechanism* with placeholder copy.

- [x] **Report** UI (≤2 taps) on listings + user profiles (Session 25) — mobile `ReportDialog` made self-contained ({targetType,targetId} + auth/error/duplicate states) → `api.reportListing`/`api.createReport`; wired on listing detail (`target_type=listing`) and public profile (`target_type=user`). Reasons + i18n reuse the shared `report.*` catalog.
- [x] **(me+backend)** **Report a message** in chat (Session 25) — long-press a chat bubble → report `target_type=message`; chat header ⋮ → report `target_type=conversation`. **Server-side authz added:** `ReportsService` verifies the reporter is a conversation participant (service-role path) **and** the reports INSERT RLS enforces the same for message/conversation (direct path, 0018); a partial unique index rejects duplicate PENDING reports (409/23505). Web admin queue now resolves message reports (author + body snippet + link; "(message deleted)" when gone).
- [x] **Block/unblock** on every profile + in chat + a Blocked-users screen (Session 25) — block toggle on public profile (Ban/Undo2, refetches now-hidden listings) + chat header ⋮ block; `app/blocked.tsx` list with per-row unblock; Profile-tab entry. Reuses the existing `blocks` table/RLS/triggers (sever-follows; unblock does NOT restore follows). `api.block`/`unblock`/`blockedUsers` + direct `isBlocked`.
- [~] **(me+backend)** **Proactive content filter** on every UGC write — **provider-agnostic seam built** (`ModerationModule` / `ContentModerationService`, honest no-op today, wired at listing-create; flags → hidden-pending-review when a provider is wired). **Decision D-2 = OWNER CALL, brief written:** `docs/d2-content-moderation.md` (text/image/CSAM options, live 2026 pricing + Arabic quality, recommendation: OpenAI free text + Rekognition/Google images + Cloudflare/PhotoDNA CSAM). CSAM detection + reporting is separate (SLA §6 + brief §4).
- [x] Reactive moderation already live (auto-hide at 5 reporters + admin ban/suspend/remove — `BUILD_PLAN.md` Phase 4). Reuse.
- [x] **(me)** Published contact/support screen (Session 25) — `app/support.tsx` (mailto `support@justswap.me`, topics, links to Terms) + `app/terms.tsx` viewer (renders the shared `terms.sections`); Profile-tab "Contact support" entry. Copy single-sourced from the ported catalog.
- [x] **(me+backend)** **Persist ToS acceptance** (Session 25) — migration **0018** adds `profiles.terms_accepted_version`/`terms_accepted_at` + `current_terms_version()`/`has_accepted_current_terms()`; `TERMS_VERSION` in `@swap/config`. **Server-stamped** via `POST /me/terms` (client can't spoof). Enforced defence-in-depth: NestJS `TermsGuard` on listing/message/proposal/rating/image endpoints **+** listings/messages INSERT RLS. Mobile `TermsProvider.ensureAccepted()` hard-blocks all 9 UGC write sites with a zero-tolerance acceptance sheet; re-consent on version bump. **Applied to the live DB + verified.**
- [x] Document the **24h remove-content-and-eject-user** moderation SLA (Session 25) — `docs/moderation-sla.md` (intake → queue → actions → audit log → CSAM escalation to the UAE authority/NCMEC → human responsibilities + reviewer-notes summary).

**Exit criterion:** [~] **Four of five pillars demonstrable in the app + verified (Session 25):** Report on listing/user/message/conversation ✅ · Block hides a user ✅ · contact info visible ✅ · EULA accepted-before-posting ✅ (live: gate → accept → server-stamp → write proceeds). Automated: **9/9 security tests** (5 RLS + 4 API; `apps/api/test/m4-trust-safety.mjs`) + typecheck 8/8 + migration 0018 applied/verified. **Pillar 5 (proactive filter) remains OPEN on Decision D-2** (owner picks a provider; seam + brief ready). Also owner/Phase-S: Google Play Child-Safety declaration + public CSAE URL. Backend note: `terms_accepted`/RLS on avatar/profile edits is client-gate-only (the acceptance write is itself a profile update — chicken-and-egg; listings/messages carry the RLS gate).

---

> ## ── HARD STOP ──  ⚠ Register the **Apple Developer account** ($99/yr) to do the iOS half of M5–M6.
> Everything above (M0–M4) and **all of Android** below needs no account. iOS push (APNs) and iOS device/TestFlight builds do. See "The one hard stop" above.

---

### Phase M5 — Native Integrations `(me)` — Android fully local; iOS push/device needs the Apple account
> All require a dev/production build (none work in Expo Go).

- [x] **Camera / image capture** (Session 26) · `acquireImages()` adds a Take-Photo/Choose-Library chooser + camera-permission handling (settings route on permanent denial), reusing the existing sign→uploadToSignedUrl pipeline at every image site (create/edit listing, avatar, onboarding avatar, swap-confirmation). Mic permission disabled; iOS camera/photos usage strings set via the expo-image-picker plugin. **On-device verification pending the EAS rebuild.**
- [~] **(me+backend)** **Push** (Session 26) · **Foundation built + tested (7/7).** migration 0020 (`device_tokens` owner-only RLS + `push_outbox` service-role queue + enqueue-on-notification trigger — never sends inline), NestJS `PushModule` (register/revoke/list `/me/devices` + `processOutbox` worker with exp-backoff + invalid-token disable, `MockPushProvider`, @Cron gated OFF by `PUSH_WORKER_ENABLED`, admin `/admin/push/process`), mobile `push.ts` + `PushManager` (register on auth, revoke on logout, tap→route). **Remaining (owner):** FCM v1 (Android, free Firebase) / APNs (iOS, paid Apple) creds → wire a real provider + set `PUSH_WORKER_ENABLED`.
- [~] **Deep / universal links** (Session 26) · Android `intentFilters autoVerify:true` + iOS `associatedDomains:["applinks:justswap.me"]` in app.json; `app/+native-intent.tsx` strips the web `/{locale}` prefix; `justswap://` scheme retained; web `/.well-known/assetlinks.json` + `apple-app-site-association` (verified served as `application/json`, no redirect). **Remaining (owner):** real Android SHA-256 fingerprints (`eas credentials`) + Apple Team ID → replace the documented placeholders; on-device verify after the EAS rebuild + web deploy.
- [x] **Biometrics** (Session 26) · `expo-local-authentication` opt-in app-lock OVER the session (never stores the password), flag in SecureStore, device-credential fallback, foreground re-lock, Sign-out escape; Profile toggle; `NSFaceIDUsageDescription` set. **On-device verification pending the EAS rebuild.**
- [x] **Share sheet** (Session 26) · RN `Share` (`shareListing`/`shareProfile`) with canonical `https://justswap.me/{locale}/…` links + header buttons on listing + profile; localized copy. (No new native module — verifiable on any build.)

**Exit criterion:** [~] **Code + backend complete for all five; on-device Android verification pending one EAS dev-client rebuild** (the new native modules — notifications / local-authentication / secure-store — aren't in the pre-M5 client). Automated: typecheck 8/8, expo-doctor clean, **m5-push 7/7**, migrations 0019+0020 applied to the live DB, well-known files served correctly. **Owner-gated for full sign-off:** FCM/APNs creds (real push), App-Link SHA-256/Team-ID (verified links). iOS device/APNs remain behind the paid Apple account (M5 hard stop).

---

### Phase M6 — Build Config (EAS) `(me)` — Android builds account-free; iOS signed build needs the Apple account
- [~] Author **`eas.json`**: `development` (dev-client, internal), `preview` (internal, APK), `production` (store, AAB) + a `submit.production` block (iOS ASC API key fields; Android service-account/track/releaseStatus). **Session 20:** linked `@most.othman/justswap` and verified the Android development APK via EAS build `66e95688-e3de-48e1-ae29-db3a917265fc`.
- [ ] App-config plugins + all permission strings; **`ios.privacyManifests`** (`PrivacyInfo.xcprivacy`) with required-reason declarations (audit each native dep — a known 2026 upload-rejection cause).
- [ ] Icons/splash/graphics: iOS icon **1024×1024, no alpha, no rounded corners** *(current `assets/icon.png` likely carries alpha — flatten it)*; Google icon **512×512 32-bit PNG w/ alpha ≤1 MB**; **Google feature graphic 1024×500 (required)**.
- [ ] **Android production AAB builds locally / via EAS with no account.** iOS production build is authored here but **produces an installable binary only after the Apple account exists** (Phase S credentials).

**Exit criterion:** `eas build -p android --profile production` succeeds and installs on a device with no account; the iOS build config is complete and ready for signing once the Apple account is registered.

---

> ## ══ HARD STOP — everything below is BLOCKED ON YOU (accounts, legal, console, submission) ══

### Phase S — Store Registration, Compliance & Submission `(you)` `(me: drafts + assets)`
> Do this after the apps are built. All copy/screenshots in **Arabic + English**. See the Blocked-on-you table for IDs.

**Accounts & credentials** — `B-ACC-*`, `B-CRED-*`
- [!] Register **Apple Developer Program** ($99/yr) — needed from M5 for iOS; **Individual vs Organization is Decision D-5 (open)**.
- [!] Register **Google Play Console** ($25 one-time).
- [!] Signing/submit credentials: Apple ASC API key (`.p8` + Key/Issuer IDs); Google Play service-account JSON; APNs key + FCM v1 (push). EAS can manage device-signing.

**Legal & compliance content** — `B-LEGAL-*`, `B-STORE-*` (me drafts → you submit/host)
- [!] Host **privacy policy** + **terms/EULA** (bilingual, reachable) at justswap.me — both stores hard-require the privacy URL; Apple 1.2 needs the accepted EULA (M4 gate points here).
- [!] Apple **App Privacy** nutrition labels + Google **Data Safety** form (from my data-map draft).
- [!] Apple **age rating** (new 4+/9+/13+/16+/18+; UGC+messaging → expect ≥13+; **Social-Media descriptor required from Sept 2026**) + Google **IARC** content rating.

**Store listing & assets** (me generates → you approve/upload)
- [ ] Listing copy per store/locale: name (≤30), Apple subtitle (30)/Google short desc (80), description (4000), Apple keywords (100), promo text (170).
- [ ] Screenshots, RTL-mirrored for Arabic: Apple **iPhone 6.9″ 1320×2868** (+ iPad 13″ 2064×2752 if enabled); Google **phone ≥1080×1920** + **feature graphic 1024×500**. No alpha.

**Pre-release gate & submit** — `B-STORE-3/4`, `B-SUBMIT-1`
- [!] **TestFlight** internal + **Google Play internal testing** pass a real-device run of the M3 lifecycle + M4 safety flows. *(New personal Google accounts: 12 testers / 14 continuous days before production — Org accounts exempt.)*
- [!] Apple **demo account** (buyer/seller/admin) in review notes; keep backend live during review.
- [ ] **(me)** Draft App-Review reviewer notes (exact taps to Report/Block/EULA/contact + the 24h SLA).
- [!] `eas submit` → **Submit for Review** on both stores; respond to any rejection.

**Exit criterion (whole plan):** Live on **both** the App Store and Google Play, installable on device, full swap lifecycle functional on mobile (satisfies `BUILD_PLAN.md` Phase-7 exit) with all five Apple-1.2 pillars in place.

---

### Blocked on you (human-only) — consolidated

| ID | Item | Cost / lead time | Unblocks |
|---|---|---|---|
| **B-ACC-1** | Apple Developer Program enrollment | $99/yr; D-U-N-S can take days if Org | iOS push (M5), iOS build (M6), submit (S) |
| **B-ACC-2** | Google Play Console account | $25 one-time | Play distribution (S) |
| **B-ACC-3** | D-U-N-S number (only if Org — see D-5) | Free; days | Apple Org enrollment |
| **B-CRED-1** | Apple ASC API key (`.p8` + Key/Issuer IDs) | — | EAS Submit iOS |
| **B-CRED-2** | Google Play service-account JSON | — | EAS Submit Android |
| **B-CRED-3** | APNs key + FCM v1 service-account | — | Push (M5) |
| **B-LEGAL-1** | Privacy policy URL (bilingual) at justswap.me | — | Both stores (S) |
| **B-LEGAL-2** | Terms/EULA w/ zero-tolerance clause (bilingual), hosted | — | Apple 1.2 gate (M4), S |
| **B-STORE-1** | Fill App Privacy + Data Safety forms (my draft) | — | S |
| **B-STORE-2** | Apple age rating + Google IARC questionnaires | — | S |
| **B-STORE-3** | Google closed test: ≥12 testers / ≥14 days (new personal accounts only; Org exempt) | 14+ days | Play production |
| **B-STORE-4** | Apple demo account (buyer/seller/admin) + live backend during review | — | Apple review |
| **B-SUBMIT-1** | Submit for Review on both stores; handle rejections | Apple review ~24–48h | Launch |
| **B-DEC** | Resolve open decisions (esp. D-2 at M4, D-5 before S) | — | as noted |

---

## Store-requirement facts (verify the ⚠ before submitting — current 2026-07)

- **Apple build gate (now):** uploads must be built with **Xcode 26 / iOS 26 SDK** (since 2026-04-28). Compile-time only — deployment target can stay iOS 15+. **Expo SDK 57 (now in use) meets this.** SDK 51 did **not** (upgraded in M0). ([apple upcoming-requirements](https://developer.apple.com/news/upcoming-requirements/?id=02032026a), [expo sdk-54](https://expo.dev/changelog/sdk-54))
- **Google target API:** **API 35** required now; **⚠ API 36 required from 2026-08-31** (extension to 2026-11-01). Current Expo SDK handles `targetSdkVersion`. **16 KB page size** required for API-35+ apps with native code since 2025-11-01 (RN/Expo `.so` libs — reason to stay current). ([android target-sdk](https://developer.android.com/google/play/requirements/target-sdk), [16 KB](https://developer.android.com/guide/practices/page-sizes))
- **Apple 1.2 pillars:** filter · report + 24h action · block · published contact · accepted zero-tolerance EULA. Disclaimers don't count — reviewers exercise the UI. ([§1.2](https://developer.apple.com/app-store/review/guidelines/))
- **Accounts:** Apple $99/yr (Org needs D-U-N-S); Google $25 one-time; new personal Google accounts need 12-tester/14-day closed testing (Org exempt). ([google testing](https://support.google.com/googleplay/android-developer/answer/14151465))
- **Screenshots (⚠ changed):** Apple **6.9″ 1320×2868** (+ iPad 13″ 2064×2752); the rest auto-scale. Google **phone ≥1080×1920** + **feature graphic 1024×500**. No alpha; per-locale sets (Arabic from the RTL build). ([apple](https://developer.apple.com/help/app-store-connect/reference/screenshot-specifications/), [google](https://support.google.com/googleplay/android-developer/answer/9866151))
- **Privacy manifest:** ship `ios.privacyManifests`; audit non-Expo native deps' required-reason declarations. ([expo](https://docs.expo.dev/guides/apple-privacy/))

---

## Open Decisions

| # | Question | Status |
|---|---|---|
| **D-4** | Bundle id / brand name | ✅ **RESOLVED (2026-07-22): `me.justswap.app`** (reverse-DNS of the owned domain `justswap.me`) for both platforms; display name **"JustSwap"** (one word). Set in `app.json` in M0. `justswap.me` is also the Universal-Links / App-Links domain (M5). |
| **D-7** | Mobile RTL direction strategy + first-launch invariant | ✅ **RESOLVED + IMPLEMENTED (Sessions 10–11, 2026-07-23).** **Strategy: native `I18nManager.forceRTL` flag**, not JS-managed Yoga `direction` (which silently breaks native-stack nav / horizontal-scroll / TextInput caret with no JS remedy; the 18 primitives were never the cost — 5-agent research + audit). **Locale rule:** Arabic device ⇒ Arabic/RTL; **any other language ⇒ English/LTR**. **Hard invariant (user, non-negotiable): a mismatched direction must never render, not even one frame.** Enforced by (1) an **Android native early-set** — config plugin `plugins/withAndroidRtlEarlySet.js` injects into `MainApplication.onCreate` a `forceRTL(device-lang == "ar")` **before React loads**, so the first surface is always correct with **no reload** (works in a bare production release); + (2) a splash-gated **JS boot guard** (`app/_layout.tsx`: `SplashScreen.preventAutoHideAsync` → reconcile native flag vs locale → reveal, else `forceRTL`+`reloadAsync`) as a dev-path + safety net. **Verified on-device (RN 0.86 Android):** `ar`→RTL Arabic, `en`→LTR, **Urdu (RTL-script-non-ar)→LTR English** (the override case), all `bundle-loads=1` (no guard reload). **iOS:** `app.json → ios.infoPlist.CFBundleLocalizations:['ar','en']` base `en` aligns every iOS case natively (Arabic→RTL, else→LTR) — **UNVERIFIED till M5** (no Mac; the JS guard is the runtime backstop). **Key finding:** `Updates.reloadAsync()` **throws `ERR_UPDATES_DISABLED` in a bare production release** (needs EAS Update: url+runtimeVersion) — so the boot invariant uses the native early-set, not a reload. **M3 open — the in-app switcher (Option A) restart is ASYMMETRIC across platforms** (record now, decide at M3): the runtime language flip must re-apply the native flag, but there is no single cross-platform mechanism. **Android:** a native activity restart works. **iOS:** there is **NO Apple-sanctioned programmatic restart** — `exit(0)` reads as a crash and risks App Store rejection, and `react-native-restart` is unmaintained with no New-Arch support. So a native activity restart covers Android with **no iOS equivalent.** The real iOS options are: **(a) enable EAS Update** (`reloadAsync` then works on both platforms), **(b) custom native bridge-reload work** (recreate the RN surface natively on iOS), or **(c) a manual "reopen JustSwap to apply" prompt** (no auto-restart). Not decided — bring options at M3. Also: the early-set must then read the *persisted user choice*, not just the device locale. |
| **D-5** | Apple/Google enrollment — Individual or Organization? | ⏸ **OPEN — deliberately deferred (2026-07-22).** JustSwap is a standalone project; **no existing company is assumed.** Decide at Phase S. Trade-off to weigh then: Org needs a D-U-N-S (lead time) but gets team roles and skips Google's 12-tester gate; Individual is faster but publishes under a personal legal name. **Does not block the build (M0–M6).** |
| **D-2** | Proactive content-moderation approach (Apple 1.2 Pillar 3) | ⏳ **AWAITING OWNER DECISION — brief delivered (Session 25, 2026-08-05).** Full comparison in **`docs/d2-content-moderation.md`** with live-verified 2026 pricing + Arabic quality: **text** (OpenAI Moderation = free & multilingual, Hive/Google NL for stronger Arabic, avoid Perspective — sunsets Dec 2026, AWS = English-only), **images** (AWS Rekognition $1/1k or Google Vision SafeSearch), **CSAM** (legally distinct — Cloudflare free/self-serve + PhotoDNA/Google partner tools; UAE Decree-Law 26/2025 reporting). **Recommendation:** hybrid — OpenAI (free text) + Rekognition/Google (images) + Cloudflare/PhotoDNA (CSAM) + a human-review queue. **Provider-agnostic seam is BUILT** (`ContentModerationService`, no-op until wired). **Owner must:** pick the stack, authorize provider account(s)/keys, confirm the UAE reporting authority with counsel, and (Phase S) host the CSAE URL + file Google Play's Child-Safety declaration. This is the ONE open M4 blocker. |
| **D-1** | Mobile browse data path | **Recommend (unconfirmed): direct `@swap/api` RLS queries**, not REST browse (which skips block-filtering — `BUILD_PLAN.md` Known-Issue). M0 harness already uses the RLS path. Confirm in M1. |
| **D-3** | Admin (dashboard/moderation/catalog) on mobile, or web-only? | **Recommend (unconfirmed): web-only for v1.** Consumer safety UI (report/block/blocked-list) is in mobile scope regardless. |
| **D-6** | Push transport — Expo Push vs direct FCM/APNs? | **Recommend (unconfirmed): Expo Push** (simplest with `expo-notifications`; still needs FCM v1 + APNs creds). |

---

## Known gaps / risks

- [RESOLVED] ~~Local Android build needs Windows Long Paths~~ — RN New-Arch codegen produces ~**373-char** object paths (> Windows **260**). **Two fixes, both required:** (1) `LongPathsEnabled=1` (HKLM, elevated — done by the user via `New-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem' -Name 'LongPathsEnabled' -Value 1 -PropertyType DWord -Force`); and (2) the CMake-bundled **ninja 1.10.2 is not long-path-aware**, so the OS flag alone still failed → replaced `Sdk/cmake/3.22.1/bin/ninja.exe` with **ninja 1.12.1** (`\\?\` long-path support; original saved as `ninja.exe.orig`). Build then **succeeded** + M0 smoke 5/5. **⚠ Machine-setup note (not committable — lives in the Android SDK, not the repo):** a fresh Windows machine needs BOTH long paths enabled AND ninja ≥1.12 in `cmake/<ver>/bin/`. Mac/Linux/EAS have no 260 limit → Windows-local only. — added 2026-07-22, resolved 2026-07-22
- [DECISION] **`nodeLinker` stays pnpm's default `isolated` (strict, phantom-dep-safe).** Briefly tried `nodeLinker: hoisted` to shorten `.pnpm` paths, then **reverted** — it's a global resolution change for a Windows-local problem and it drops pnpm's phantom-dep protection (apps/web + apps/api deploy from this repo via `pnpm install --frozen-lockfile`). The mobile build succeeds under **isolated** anyway (ninja 1.12.1 handles the long paths; the only failure was a clang OOM from low free RAM, fixed by capping native parallelism). Verified under isolated: **web `next build`** ✓, **api `tsc` build** ✓, **api `ts-node` runtime** (boots, all modules resolve, `/health`+routes 200) ✓ → no phantom deps. Machine-local tunings live in git-ignored `apps/mobile/android/gradle.properties`: `reactNativeArchitectures=x86_64` (fast emulator builds; EAS/prod uses all ABIs) + `org.gradle.parallel=false` / `org.gradle.workers.max=4` (RAM). — 2026-07-22
- [RESOLVED] ~~iOS did not declare Arabic as a supported localization~~ — added `ios.infoPlist.CFBundleLocalizations:['ar','en']` (base `en`) in `app.json`. iOS now selects Arabic (→RTL) for Arabic devices and English (→LTR) for every other language, matching the JS locale rule with no reload. **UNVERIFIED till M5** (no Mac); the JS boot guard is the runtime backstop. Android was already correct (`android:supportsRtl='true'`). — 2026-07-23 (D-7)
- [RESOLVED] ~~clang OOM on the cold New-Arch native build~~ (recurred when installing `expo-updates` invalidated the CMake cache → full codegen recompile at 12-way parallelism on ~1.9 GB free RAM). **Fix: a CMake job pool** in git-ignored `android/app/build.gradle` (`-DCMAKE_JOB_POOLS:STRING=compile_pool=2;link_pool=1`) caps concurrent compile/link jobs regardless of ninja's `-j` — the gradle `workers.max` cap does **not** reach ninja's internal parallelism (why it recurred). Also pin `sdk.dir` in git-ignored `android/local.properties` (fresh shells have no `ANDROID_HOME`). Both machine-local. Build then succeeded (3m29s). — 2026-07-23
- [NOTE] **`expo-updates` installed** (`~57.0.9`) but the **boot direction guard does NOT depend on it** — `reloadAsync()` throws `ERR_UPDATES_DISABLED` in a bare production release (needs EAS Update url+runtimeVersion), so the invariant is enforced by the **Android native early-set** instead (D-7). expo-updates stays for the **M3 switcher** (Option A) + future OTA; `reloadAsync` works in dev and will work in prod if/when EAS Update is enabled. — 2026-07-23
- [NOTE — every `forceRTL`/`allowRTL` call site (D-7)] The native direction flag is set in exactly two places, both deriving from the same rule (Arabic ⇒ RTL, else ⇒ LTR): (1) **Android native early-set** — `MainApplication.onCreate` via plugin `withAndroidRtlEarlySet`, before React loads, every launch, from the **device** locale — the production mechanism; (2) **JS boot guard** — `app/_layout.tsx`, only if native ≠ required (shouldn't happen given #1), then `forceRTL` + `reloadAsync` (works in dev; the reload throws in bare prod but this path shouldn't trigger). `src/i18n/index.ts` **no longer** touches `I18nManager`. **Future:** the M3 in-app switcher adds a third site and must feed the *persisted user choice* into #1. — 2026-07-23
- [NOTE — config plugin + prebuild] The RTL early-set is a committed Expo config plugin (`apps/mobile/plugins/withAndroidRtlEarlySet.js`; requires `expo/config-plugins`, **not** `@expo/config-plugins` — the latter is a phantom dep under pnpm's isolated linker). It applies on `expo prebuild` / EAS. On this machine it's ALSO applied directly to the git-ignored `MainApplication.kt` so local `gradlew` builds have it with no prebuild. **⚠ A future `expo prebuild` re-injects the plugin but REGENERATES `android/`, wiping the machine-local build tunings** (gradle.properties RAM/ABI caps, `app/build.gradle` CMake job pool, `local.properties`) — re-apply them, or promote them to plugins. — 2026-07-23
- [RESOLVED] ~~[ACTION — native Auth mail callbacks]~~ Session 22's `authCallbackUrl()` sends signup/resend and recovery links to `justswap://auth/callback?next=…`, where the Expo route verifies the Supabase `token_hash` and opens native onboarding/reset. The `justswap://**` Redirect URL allow-list was configured, and **Session 24 live-verified real signup-confirm, resend, and recovery mail links end-to-end** (real `token_hash` → `verifyOtp` → native onboarding/reset; `updateUser` happy-path via Node). — 2026-07-25, resolved 2026-07-30
- [NOTE — backend denorm] The `profiles.listings_count` denormalized stat is **not decremented when a listing is soft-deleted** (observed Session 24 during the owner-management pass — deleting a listing left the profile count unchanged). This is a DB-trigger nuance on the shared backend, **not a mobile bug**; flag to backend if the profile stat should reflect only active listings. — 2026-07-30
- [NOTE — dev env, Windows emulator] On this machine the Android emulator must run with **`-gpu auto`** (real host GPU / NVIDIA) to render the Fabric app; **`-gpu swiftshader_indirect`** (software) shows a white screen. **`uiautomator` is unreliable here** (reports an empty view hierarchy even while the app renders) — trust **screenshots** for on-device verification, not uiautomator. `adb` deep-link testing: single-quote the URL inside `adb shell` (`am start … -d 'URL' …`) or the shell truncates it at the first `&`, dropping `token_hash`/`type`/`next`. — 2026-07-30
- [RESOLVED] ~~Expo SDK 51/RN 0.74 not submittable~~ — **upgraded to SDK 57** (RN 0.86, New Arch on) in M0 (Session 2). — 2026-07-22
- [RESOLVED] ~~`createSupabaseClient` had no storage adapter → cold-start logout on native~~ — **fixed** (accepts `storage`), mobile injects AsyncStorage. — 2026-07-22
- [RESOLVED] ~~`react-native-url-polyfill` absent~~ — added + imported at app entry. — 2026-07-22
- [NOTE] `expo-doctor` reports one "duplicate react" (mobile React 19.2.3 vs web React 18.3.1). **Expected & non-blocking:** web is Next/React 18, mobile is Expo/React 19; pnpm isolates them, so the mobile bundle contains only 19.2.3. Not fixable without a separate (risky) web React 18→19 upgrade — out of scope. — 2026-07-22
- [OPEN — Apple 1.2 / D-2] **No proactive content filter wired yet** (text/image) — the ONE open M4 pillar. The provider-agnostic seam (`ContentModerationService`) is built + a no-op; the D-2 brief (`docs/d2-content-moderation.md`) has the options/pricing + recommendation. Owner picks a provider, then wire the seam. — 2026-07-22, brief 2026-08-05
- [DONE — M4, Session 25] **Persisted versioned Terms/EULA acceptance** shipped: migration 0018 + `TermsGuard` + RLS + mobile `TermsProvider`. **Migration 0018 was applied to the LIVE DB** (additive; all existing users' `terms_accepted_version` = NULL → they re-consent on their next UGC write, by design). Deploy ordering: the migration must precede the API/mobile deploy (the guard reads the column). — 2026-08-05
- [NOTE — M4] **Terms enforcement on avatar/profile-edit is CLIENT-gate-only** (no RLS gate): the acceptance write is itself a `profiles` update, so an RLS terms-gate on `profiles` would be chicken-and-egg. Listings + messages carry the RLS gate; profile/avatar rely on the mobile `ensureAccepted()` gate + (web) leave PATCH /me ungated. Acceptable — the high-value public "posting" surfaces are RLS-enforced. — 2026-08-05
- [NOTE — M4] **`ContentModerationService` returns a `scanned:false` pass-through** today (no provider) — it is NOT a fake "approved". Reactive report + auto-hide remain the safety net until D-2 wires a provider (then flagged listings are created `hidden`). — 2026-08-05
- [OPEN — Phase S] **Google Play Child-Safety Standards declaration** (a missing form BLOCKS Play updates) + a **public CSAE standards URL** + a named child-safety point of contact. Owner + hosting. — 2026-08-05
- [OPEN — Apple 1.2] **ToS acceptance not persisted** (web checkbox only, no DB column). Needs a migration in M4. — 2026-07-22
- [OPEN] **No message-level report UI** on any platform (schema supports it). Add for mobile in M4. — 2026-07-22
- [OPEN] Block-filtering not applied on the REST/service-role browse path (`BUILD_PLAN.md` Known-Issue) — mobile must browse via the RLS query layer (D-1). — 2026-07-22
- [OPEN — M6] iOS app icon likely carries alpha — flatten (no alpha, no rounded corners) or Apple rejects. — 2026-07-22
- [WATCH] Google **API-36 deadline 2026-08-31** + 16 KB page size — stay on a current Expo SDK; re-verify at submission. — 2026-07-22
- [INFO] `EXPO_PUBLIC_API_URL` defaults to `http://10.0.2.2:4000/api/v1` (Android-emulator→host). Real devices need the LAN IP or the deployed API URL; Supabase auth/reads/realtime hit the remote host directly regardless. — 2026-07-22

---

## Agent Instructions for Updating This File

**At the start of every mobile session:**
1. Read "RESUME HERE" and the current phase's checklist.
2. Check **Open Decisions** — D-2 blocks M4; D-5 blocks Phase S enrollment; don't guess, get the call.
3. Re-verify any ⚠ store fact against its source — store rules change monthly.

**At the end of every mobile session:**
1. Update `[x]`/`[~]`/`[ ]`/`[!]` on every item touched.
2. Update RESUME HERE (Current phase / Last worked on / Next task).
3. Add a Session Log row (# + one-line summary + date).
4. Add any new decision/gap; resolve decisions you got a call on.
5. Mirror status to `BUILD_PLAN.md` **Phase 7** so the two stay consistent (this plan is the detail; Phase 7 is the summary).
